# hvalue.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `Sizeof()`: Performs Sizeof operation in hvalue.cpp
- `Ptr()`: Performs Ptr operation in hvalue.cpp
- `WriteToBuff()`: Performs WriteToBuff operation in hvalue.cpp
- `ReadFromBuff()`: Performs ReadFromBuff operation in hvalue.cpp
- `DoPtrData()`: Performs DoPtrData operation in hvalue.cpp
- `DoSizeofData()`: Performs DoSizeofData operation in hvalue.cpp
- `isSimple()`: Checks a condition in hvalue.cpp and returns the result
- `isReal()`: Checks a condition in hvalue.cpp and returns the result
- `isFixed()`: Checks a condition in hvalue.cpp and returns the result
- `isVariable()`: Checks a condition in hvalue.cpp and returns the result
### Summary
The `hvalue.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `Sizeof()`: Performs Sizeof operation in hvalue.cpp
- `Ptr()`: Performs Ptr operation in hvalue.cpp
- `WriteToBuff()`: Performs WriteToBuff operation in hvalue.cpp
- `ReadFromBuff()`: Performs ReadFromBuff operation in hvalue.cpp
- `DoPtrData()`: Performs DoPtrData operation in hvalue.cpp
- `DoSizeofData()`: Performs DoSizeofData operation in hvalue.cpp
- `isSimple()`: Checks a condition in hvalue.cpp and returns the result
- `isReal()`: Checks a condition in hvalue.cpp and returns the result
- `isFixed()`: Checks a condition in hvalue.cpp and returns the result
- `isVariable()`: Checks a condition in hvalue.cpp and returns the result
### Podsumowanie
Plik `hvalue.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
