# codepage.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `installed_codepages`: Implements installed_codepages functionality
### Key Functions
- `GetACP()`: Processes input by dispatching to different code paths based on type or value
- `GetOEMCP()`: Processes input by dispatching to different code paths based on type or value
- `oem()`: Processes input by dispatching to different code paths based on type or value
- `ansi()`: Processes input by dispatching to different code paths based on type or value
- `extract_codepage_name()`: Processes input by dispatching to different code paths based on type or value
- `callback()`: Implements callback functionality
- `enum_cp_callback()`: Validates conditions and throws exceptions when errors are detected during enum_cp_callback operation
- `cpp_try()`: Implements cpp_try functionality
- `get_installed_codepages()`: Retrieves _installed_codepages from current context or object state
- `InstalledCodepages()`: Implements InstalledCodepages functionality
### Summary
The `codepage.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `installed_codepages`: Implements installed_codepages functionality
### Kluczowe Funkcje
- `GetACP()`: Processes input by dispatching to different code paths based on type or value
- `GetOEMCP()`: Processes input by dispatching to different code paths based on type or value
- `oem()`: Processes input by dispatching to different code paths based on type or value
- `ansi()`: Processes input by dispatching to different code paths based on type or value
- `extract_codepage_name()`: Processes input by dispatching to different code paths based on type or value
- `callback()`: Implements callback functionality
- `enum_cp_callback()`: Validates conditions and throws exceptions when errors are detected during enum_cp_callback operation
- `cpp_try()`: Implements cpp_try functionality
- `get_installed_codepages()`: Retrieves _installed_codepages from current context or object state
- `InstalledCodepages()`: Implements InstalledCodepages functionality
### Podsumowanie
Plik `codepage.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
