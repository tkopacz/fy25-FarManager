# pathutils.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `long_path()`: Performs long_path operation as part of the component's functionality
- `long_path_norm()`: Performs long_path_norm operation as part of the component's functionality
- `add_trailing_slash()`: Adds new _trailing_slash to collection or list
- `del_trailing_slash()`: Performs del_trailing_slash operation as part of the component's functionality
- `locate_path_root()`: Performs locate_path_root operation as part of the component's functionality
- `extract_path_root()`: Performs extract_path_root operation as part of the component's functionality
- `extract_file_name()`: Performs extract_file_name operation as part of the component's functionality
- `extract_file_path()`: Performs extract_file_path operation as part of the component's functionality
- `extract_file_ext()`: Performs extract_file_ext operation as part of the component's functionality
- `is_root_path()`: Tests whether _root_path condition is true or property exists
### Summary
The `pathutils.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `long_path()`: Performs long_path operation as part of the component's functionality
- `long_path_norm()`: Performs long_path_norm operation as part of the component's functionality
- `add_trailing_slash()`: Dodaje nowy _trailing_slash do kolekcji lub listy
- `del_trailing_slash()`: Performs del_trailing_slash operation as part of the component's functionality
- `locate_path_root()`: Performs locate_path_root operation as part of the component's functionality
- `extract_path_root()`: Performs extract_path_root operation as part of the component's functionality
- `extract_file_name()`: Performs extract_file_name operation as part of the component's functionality
- `extract_file_path()`: Performs extract_file_path operation as part of the component's functionality
- `extract_file_ext()`: Performs extract_file_ext operation as part of the component's functionality
- `is_root_path()`: Testuje czy _root_path warunek jest prawdziwy lub właściwość istnieje
### Podsumowanie
Plik `pathutils.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
