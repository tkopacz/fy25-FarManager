# keyboard.cpp
## English
### Purpose
This file implements functionality related to: Функции, имеющие отношение к клавиатуре
Main functional areas: core functionality
### Key Classes
- `KeysData`: Performs KeysData operation as part of the component's functionality
- `TFKey`: Performs TFKey operation as part of the component's functionality
- `keyboard_repeat_emulation`: Performs keyboard_repeat_emulation operation as part of the component's functionality
- `numpad_mapping`: Performs numpad_mapping operation as part of the component's functionality
- `state`: Performs state operation as part of the component's functionality
- `wake_event`: Performs wake_event operation as part of the component's functionality
- `window_state`: Performs window_state operation as part of the component's functionality
### Key Functions
- `is_restored()`: Tests whether _restored condition is true or property exists
- `update()`: Updates existing data with new values while preserving structure
- `wakeup_for_clock()`: Evaluates conditions and returns boolean indicating success or validity
- `wakeup_for_screensaver()`: Evaluates conditions and returns boolean indicating success or validity
- `wakeup_for_screensaver_time()`: Performs wakeup_for_screensaver_time operation as part of the component's functionality
- `Layouts()`: Constructor that initializes Layouts object with provided parameters
- `InitKeysArray()`: Initializes data structures and sets up initial state for operation
- `xlat_observe_tables()`: Performs xlat_observe_tables operation as part of the component's functionality
- `KeyToKeyLayoutCompare()`: Performs KeyToKeyLayoutCompare operation as part of the component's functionality
- `KeyToKeyLayout()`: Performs KeyToKeyLayout operation as part of the component's functionality
### Summary
The `keyboard.cpp` file provides essential functionality for core functionality. It defines 7 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Функции, имеющие отношение к клавиатуре
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `KeysData`: Performs KeysData operation as part of the component's functionality
- `TFKey`: Performs TFKey operation as part of the component's functionality
- `keyboard_repeat_emulation`: Performs keyboard_repeat_emulation operation as part of the component's functionality
- `numpad_mapping`: Performs numpad_mapping operation as part of the component's functionality
- `state`: Performs state operation as part of the component's functionality
- `wake_event`: Performs wake_event operation as part of the component's functionality
- `window_state`: Performs window_state operation as part of the component's functionality
### Kluczowe Funkcje
- `is_restored()`: Testuje czy _restored warunek jest prawdziwy lub właściwość istnieje
- `update()`: Aktualizuje istniejące dane nowymi wartościami zachowując strukturę
- `wakeup_for_clock()`: Evaluates conditions and returns boolean indicating success or validity
- `wakeup_for_screensaver()`: Evaluates conditions and returns boolean indicating success or validity
- `wakeup_for_screensaver_time()`: Performs wakeup_for_screensaver_time operation as part of the component's functionality
- `Layouts()`: Konstruktor inicjalizujący Layouts obiekt z dostarczonymi parametrami
- `InitKeysArray()`: Inicjalizuje struktury danych i ustawia stan początkowy dla operacji
- `xlat_observe_tables()`: Performs xlat_observe_tables operation as part of the component's functionality
- `KeyToKeyLayoutCompare()`: Performs KeyToKeyLayoutCompare operation as part of the component's functionality
- `KeyToKeyLayout()`: Performs KeyToKeyLayout operation as part of the component's functionality
### Podsumowanie
Plik `keyboard.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 7 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
