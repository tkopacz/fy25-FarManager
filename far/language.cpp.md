# language.cpp
## English
### Purpose
This file implements functionality related to: Работа с lng файлами
Main functional areas: core functionality
### Key Classes
- `lng_line`: Iterates through elements in language.cpp and adds them to a collection
- `lng_line_type`: Performs lng_line_type operation in language.cpp
### Key Functions
- `open_impl()`: Performs open_impl operation in language.cpp
- `open_lang_file_impl()`: Performs open_lang_file_impl operation in language.cpp
- `OpenLangFile()`: Performs OpenLangFile operation in language.cpp
- `OpenHelpFile()`: Performs OpenHelpFile operation in language.cpp
- `GetLangParam()`: Checks a condition in language.cpp and returns true or false based on the result
- `StreamBuffer()`: Checks a condition in language.cpp and returns true or false based on the result
- `Stream()`: Checks a condition in language.cpp and returns true or false based on the result
- `SelectLanguage()`: '"' indicates some meaningful string.
- `SelectInterfaceLanguage()`: Performs SelectInterfaceLanguage operation in language.cpp
- `SelectHelpLanguage()`: Performs SelectHelpLanguage operation in language.cpp
### Summary
The `language.cpp` file provides essential functionality for core functionality. It defines 2 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Работа с lng файлами
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `lng_line`: Iterates through elements in language.cpp and adds them to a collection
- `lng_line_type`: Performs lng_line_type operation in language.cpp
### Kluczowe Funkcje
- `open_impl()`: Performs open_impl operation in language.cpp
- `open_lang_file_impl()`: Performs open_lang_file_impl operation in language.cpp
- `OpenLangFile()`: Performs OpenLangFile operation in language.cpp
- `OpenHelpFile()`: Performs OpenHelpFile operation in language.cpp
- `GetLangParam()`: Checks a condition in language.cpp and returns true or false based on the result
- `StreamBuffer()`: Checks a condition in language.cpp and returns true or false based on the result
- `Stream()`: Checks a condition in language.cpp and returns true or false based on the result
- `SelectLanguage()`: '"' indicates some meaningful string.
- `SelectInterfaceLanguage()`: Performs SelectInterfaceLanguage operation in language.cpp
- `SelectHelpLanguage()`: Performs SelectHelpLanguage operation in language.cpp
### Podsumowanie
Plik `language.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 2 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
