# farutils.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `Dialog`: Performs Dialog operation as part of the component's functionality
- `DialogItem`: Performs DialogItem operation as part of the component's functionality
- `DisableEvents`: Performs DisableEvents operation as part of the component's functionality
- `FileFilter`: Performs FileFilter operation as part of the component's functionality
- `MenuItems`: Performs MenuItems operation as part of the component's functionality
- `PanelItem`: Performs PanelItem operation as part of the component's functionality
- `Regex`: Performs Regex operation as part of the component's functionality
- `Selection`: Performs Selection operation as part of the component's functionality
- `Settings`: Sets or updates the tings with the provided parameter
### Key Functions
- `get_error_dlg_title()`: Retrieves _error_dlg_title from internal state or data structure
- `init()`: Initializes data structures and sets up initial state for operation
- `get_plugin_module_path()`: Retrieves _plugin_module_path from internal state or data structure
- `msg_ptr()`: Constructor that initializes msg_ptr object with provided parameters
- `get_msg()`: Retrieves _msg from internal state or data structure
- `get_optimal_msg_width()`: Retrieves _optimal_msg_width from internal state or data structure
- `message()`: Performs message operation as part of the component's functionality
- `add()`: Adds new item to collection or list
- `menu()`: Performs menu operation as part of the component's functionality
- `get_progress_bar_str()`: Retrieves _progress_bar_str from internal state or data structure
### Namespaces
- `Far`
### Summary
The `farutils.hpp` file provides essential functionality for core functionality. It defines 9 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `Dialog`: Performs Dialog operation as part of the component's functionality
- `DialogItem`: Performs DialogItem operation as part of the component's functionality
- `DisableEvents`: Performs DisableEvents operation as part of the component's functionality
- `FileFilter`: Performs FileFilter operation as part of the component's functionality
- `MenuItems`: Performs MenuItems operation as part of the component's functionality
- `PanelItem`: Performs PanelItem operation as part of the component's functionality
- `Regex`: Performs Regex operation as part of the component's functionality
- `Selection`: Performs Selection operation as part of the component's functionality
- `Settings`: Sets or updates the tings with the provided parameter
### Kluczowe Funkcje
- `get_error_dlg_title()`: Pobiera _error_dlg_title ze stanu wewnętrznego lub struktury danych
- `init()`: Inicjalizuje struktury danych i ustawia stan początkowy dla operacji
- `get_plugin_module_path()`: Pobiera _plugin_module_path ze stanu wewnętrznego lub struktury danych
- `msg_ptr()`: Konstruktor inicjalizujący msg_ptr obiekt z dostarczonymi parametrami
- `get_msg()`: Pobiera _msg ze stanu wewnętrznego lub struktury danych
- `get_optimal_msg_width()`: Pobiera _optimal_msg_width ze stanu wewnętrznego lub struktury danych
- `message()`: Performs message operation as part of the component's functionality
- `add()`: Dodaje nowy element do kolekcji lub listy
- `menu()`: Performs menu operation as part of the component's functionality
- `get_progress_bar_str()`: Pobiera _progress_bar_str ze stanu wewnętrznego lub struktury danych
### Przestrzenie nazw
- `Far`
### Podsumowanie
Plik `farutils.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 9 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
