# ExcDump.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `idLogName()`: Performs idLogName operation in ExcDump.cpp
- `idFileLog()`: Performs idFileLog operation in ExcDump.cpp
- `LogProcedure()`: Performs LogProcedure operation in ExcDump.cpp
- `HEX_SetLogProc()`: Performs HEX_SetLogProc operation in ExcDump.cpp
- `HEX_StackWalkListAddress()`: Performs HEX_StackWalkListAddress operation in ExcDump.cpp
- `MakeStackWalkListIP()`: Performs MakeStackWalkListIP operation in ExcDump.cpp
- `HEX_StackWalkListContext()`: Performs HEX_StackWalkListContext operation in ExcDump.cpp
- `MakeStackWalkListCTX()`: Performs MakeStackWalkListCTX operation in ExcDump.cpp
- `HEX_StackWalkListCurrent()`: Performs HEX_StackWalkListCurrent operation in ExcDump.cpp
- `MakeStackWalkListCUR()`: Performs MakeStackWalkListCUR operation in ExcDump.cpp
### Summary
The `ExcDump.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `idLogName()`: Performs idLogName operation in ExcDump.cpp
- `idFileLog()`: Performs idFileLog operation in ExcDump.cpp
- `LogProcedure()`: Performs LogProcedure operation in ExcDump.cpp
- `HEX_SetLogProc()`: Performs HEX_SetLogProc operation in ExcDump.cpp
- `HEX_StackWalkListAddress()`: Performs HEX_StackWalkListAddress operation in ExcDump.cpp
- `MakeStackWalkListIP()`: Performs MakeStackWalkListIP operation in ExcDump.cpp
- `HEX_StackWalkListContext()`: Performs HEX_StackWalkListContext operation in ExcDump.cpp
- `MakeStackWalkListCTX()`: Performs MakeStackWalkListCTX operation in ExcDump.cpp
- `HEX_StackWalkListCurrent()`: Performs HEX_StackWalkListCurrent operation in ExcDump.cpp
- `MakeStackWalkListCUR()`: Performs MakeStackWalkListCUR operation in ExcDump.cpp
### Podsumowanie
Plik `ExcDump.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
