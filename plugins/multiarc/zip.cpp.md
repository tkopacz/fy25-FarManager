# zip.cpp
## English
### Purpose
This file implements functionality related to: Second-level plugin module for FAR Manager and MultiArc plugin
Main functional areas: core functionality
### Key Classes
- `ExtraBlockHeader`: Iterates through collection and processes each element, storing results
- `NTFSAttributeHeader`: Iterates through collection and processes each element, storing results
- `TimesAttribute`: Iterates through collection and processes each element, storing results
- `ZIP64Descriptor`: Iterates through collection and processes each element, storing results
- `ZipHdr1`: Iterates through collection and processes each element, storing results
- `ZipHdr2`: Iterates through collection and processes each element, storing results
- `ZipHeader`: Iterates through collection and processes each element, storing results
### Key Functions
- `IsValidHeader()`: FileName[]; ExtraField[];
- `GetFilePosition()`: Iterates through collection and processes each element, storing results
- `IsZIPFileMagic()`: Iterates through collection and processes each element, storing results
- `IsArchive()`: Iterates through collection and processes each element, storing results
- `OpenArchive()`: Iterates through collection and processes each element, storing results
- `GetArcItem()`: Iterates through collection and processes each element, storing results
- `SetFilePointer()`: FileName[]; ExtraField[];
- `CloseArchive()`: Iterates through collection and processes each element, storing results
- `GetSFXPos()`: Iterates through collection and processes each element, storing results
- `GetFormatName()`: Iterates through collection and processes each element, storing results
### Summary
The `zip.cpp` file provides essential functionality for core functionality. It defines 7 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Second-level plugin module for FAR Manager and MultiArc plugin
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `ExtraBlockHeader`: Iterates through collection and processes each element, storing results
- `NTFSAttributeHeader`: Iterates through collection and processes each element, storing results
- `TimesAttribute`: Iterates through collection and processes each element, storing results
- `ZIP64Descriptor`: Iterates through collection and processes each element, storing results
- `ZipHdr1`: Iterates through collection and processes each element, storing results
- `ZipHdr2`: Iterates through collection and processes each element, storing results
- `ZipHeader`: Iterates through collection and processes each element, storing results
### Kluczowe Funkcje
- `IsValidHeader()`: FileName[]; ExtraField[];
- `GetFilePosition()`: Iterates through collection and processes each element, storing results
- `IsZIPFileMagic()`: Iterates through collection and processes each element, storing results
- `IsArchive()`: Iterates through collection and processes each element, storing results
- `OpenArchive()`: Iterates through collection and processes each element, storing results
- `GetArcItem()`: Iterates through collection and processes each element, storing results
- `SetFilePointer()`: FileName[]; ExtraField[];
- `CloseArchive()`: Iterates through collection and processes each element, storing results
- `GetSFXPos()`: Iterates through collection and processes each element, storing results
- `GetFormatName()`: Iterates through collection and processes each element, storing results
### Podsumowanie
Plik `zip.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 7 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
