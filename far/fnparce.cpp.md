# fnparce.cpp
## English
### Purpose
This file implements functionality related to: Парсер файловых ассоциаций
Main functional areas: core functionality
### Key Classes
- `brackets`: Performs brackets operation in fnparce.cpp
- `item`: Performs item operation in fnparce.cpp
- `pos_item`: Performs pos_item operation in fnparce.cpp
- `skip`: Performs skip operation in fnparce.cpp
- `subst_data`: Performs subst_data operation in fnparce.cpp
- `subst_strings`: Performs subst_strings operation in fnparce.cpp
### Key Functions
- `GetDescription()`: Retrieves Description value in fnparce.cpp
- `Default()`: Performs Default operation in fnparce.cpp
- `bool()`: Performs bool operation in fnparce.cpp
- `string_view()`: Performs string_view operation in fnparce.cpp
- `prefix()`: Performs prefix operation in fnparce.cpp
- `suffix()`: Performs suffix operation in fnparce.cpp
- `str()`: Performs str operation in fnparce.cpp
- `ProcessBrackets()`: Processes input data in fnparce.cpp
- `SkipInputToken()`: Performs SkipInputToken operation in fnparce.cpp
- `Tail()`: Performs Tail operation in fnparce.cpp
### Namespaces
- `tokens`
### Summary
The `fnparce.cpp` file provides essential functionality for core functionality. It defines 6 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Парсер файловых ассоциаций
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `brackets`: Performs brackets operation in fnparce.cpp
- `item`: Performs item operation in fnparce.cpp
- `pos_item`: Performs pos_item operation in fnparce.cpp
- `skip`: Performs skip operation in fnparce.cpp
- `subst_data`: Performs subst_data operation in fnparce.cpp
- `subst_strings`: Performs subst_strings operation in fnparce.cpp
### Kluczowe Funkcje
- `GetDescription()`: Retrieves Description value in fnparce.cpp
- `Default()`: Performs Default operation in fnparce.cpp
- `bool()`: Performs bool operation in fnparce.cpp
- `string_view()`: Performs string_view operation in fnparce.cpp
- `prefix()`: Performs prefix operation in fnparce.cpp
- `suffix()`: Performs suffix operation in fnparce.cpp
- `str()`: Performs str operation in fnparce.cpp
- `ProcessBrackets()`: Processes input data in fnparce.cpp
- `SkipInputToken()`: Performs SkipInputToken operation in fnparce.cpp
- `Tail()`: Performs Tail operation in fnparce.cpp
### Przestrzenie nazw
- `tokens`
### Podsumowanie
Plik `fnparce.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 6 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
