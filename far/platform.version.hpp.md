# platform.version.hpp

## English

### Purpose
This file implements functionality for platform-specific operations.

### Key Classes
- **`file_version`**: Implements file version functionality

### Key Functions
- **`file_version::read()`**: Performs read operation on the data stream or buffer
- **`file_version::get_string()`**: Retrieves the current string value
- **`file_version::get_fixed_info()`**: Retrieves the current fixed info value
- **`file_version::version()`**: Executes version operation
- **`file_version::description()`**: Executes description operation

### Namespaces
- `os`

### Summary
The `platform.version.hpp` file is essential for platform-specific operations. It defines 1 class(es) and implements 5 function(s) that support Far Manager file manager operations.

---

## Polski (Polish)

### Cel
Ten plik implementuje funkcjonalność dla operacji specyficznych dla platformy.

### Kluczowe Klasy
- **`file_version`**: Implements file version functionality

### Kluczowe Funkcje
- **`file_version::read()`**: Performs read operation on the data stream or buffer
- **`file_version::get_string()`**: Retrieves the current string value
- **`file_version::get_fixed_info()`**: Retrieves the current fixed info value
- **`file_version::version()`**: Executes version operation
- **`file_version::description()`**: Executes description operation

### Przestrzenie nazw
- `os`

### Podsumowanie
Plik `platform.version.hpp` jest niezbędny dla operacji specyficznych dla platformy. Definiuje 1 klas(y) i implementuje 5 funkcji wspierających operacje menedżera plików Far Manager.
