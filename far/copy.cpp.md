# copy.cpp
## English
### Purpose
This file implements functionality related to: Копирование файлов
Main functional areas: core functionality
### Key Classes
- `ShellCopy`: Performs ShellCopy operation as part of the component's functionality
- `created_folders`: Creates and initializes a new instance or resource
- `file_names_for_overwrite_dialog`: Performs file_names_for_overwrite_dialog operation as part of the component's functionality
- `overwrite`: Performs overwrite operation as part of the component's functionality
- `security`: Performs security operation as part of the component's functionality
- `total_info`: Performs total_info operation as part of the component's functionality
### Key Functions
- `copy_selected_items()`: Copies data from source to destination location
- `ShellCopyOneFile()`: Evaluates conditions and returns a boolean indicating success or validity of ShellCopyOneFile
- `CheckStreams()`: Validates data integrity and checks correctness of input
- `ShellCopyFile()`: Evaluates conditions and returns a boolean indicating success or validity of ShellCopyFile
- `ShellSystemCopy()`: Validates conditions and throws exceptions when errors are detected during ShellSystemCopy operation
- `DeleteAfterMove()`: Removes AfterMove from collection and frees associated resources
- `AskOverwrite()`: Evaluates conditions and returns a boolean indicating success or validity of AskOverwrite
- `GetSecurity()`: Retrieves Security from internal state or data structure
- `SetSecurity()`: Updates Security in internal state or configuration
- `ResetSecurity()`: Performs ResetSecurity operation as part of the component's functionality
### Summary
The `copy.cpp` file provides essential functionality for core functionality. It defines 6 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Копирование файлов
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `ShellCopy`: Performs ShellCopy operation as part of the component's functionality
- `created_folders`: Creates and initializes a new instance or resource
- `file_names_for_overwrite_dialog`: Performs file_names_for_overwrite_dialog operation as part of the component's functionality
- `overwrite`: Performs overwrite operation as part of the component's functionality
- `security`: Performs security operation as part of the component's functionality
- `total_info`: Performs total_info operation as part of the component's functionality
### Kluczowe Funkcje
- `copy_selected_items()`: Kopiuje dane ze źródła do miejsca docelowego
- `ShellCopyOneFile()`: Evaluates conditions and returns a boolean indicating success or validity of ShellCopyOneFile
- `CheckStreams()`: Waliduje integralność danych i sprawdza poprawność
- `ShellCopyFile()`: Evaluates conditions and returns a boolean indicating success or validity of ShellCopyFile
- `ShellSystemCopy()`: Validates conditions and throws exceptions when errors are detected during ShellSystemCopy operation
- `DeleteAfterMove()`: Usuwa AfterMove z kolekcji i zwalnia powiązane zasoby
- `AskOverwrite()`: Evaluates conditions and returns a boolean indicating success or validity of AskOverwrite
- `GetSecurity()`: Pobiera Security ze stanu wewnętrznego lub struktury danych
- `SetSecurity()`: Aktualizuje Security w stanie wewnętrznym lub konfiguracji
- `ResetSecurity()`: Performs ResetSecurity operation as part of the component's functionality
### Podsumowanie
Plik `copy.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 6 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
