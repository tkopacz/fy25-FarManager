# xxH.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `JMPOpCode`: Performs JMPOpCode operation as part of the component's functionality
### Key Functions
- `PushProcHookInfo()`: Performs PushProcHookInfo operation as part of the component's functionality
- `PopProcHookInfo()`: Performs PopProcHookInfo operation as part of the component's functionality
- `doBPUnwindJMP()`: Performs doBPUnwindJMP operation as part of the component's functionality
- `doBPUnwind()`: Performs doBPUnwind operation as part of the component's functionality
- `Subst_Function()`: Evaluates conditions and returns a boolean indicating success or validity of Subst_Function
- `MakeAddrWritable()`: Evaluates conditions and returns a boolean indicating success or validity of MakeAddrWritable
- `InitHook_Proc()`: Initializes data structures and sets up initial state for operation
### Summary
The `xxH.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 7 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `JMPOpCode`: Performs JMPOpCode operation as part of the component's functionality
### Kluczowe Funkcje
- `PushProcHookInfo()`: Performs PushProcHookInfo operation as part of the component's functionality
- `PopProcHookInfo()`: Performs PopProcHookInfo operation as part of the component's functionality
- `doBPUnwindJMP()`: Performs doBPUnwindJMP operation as part of the component's functionality
- `doBPUnwind()`: Performs doBPUnwind operation as part of the component's functionality
- `Subst_Function()`: Evaluates conditions and returns a boolean indicating success or validity of Subst_Function
- `MakeAddrWritable()`: Evaluates conditions and returns a boolean indicating success or validity of MakeAddrWritable
- `InitHook_Proc()`: Inicjalizuje struktury danych i ustawia stan początkowy dla operacji
### Podsumowanie
Plik `xxH.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 7 funkcji wspierających operacje menedżera plików Far Manager.
