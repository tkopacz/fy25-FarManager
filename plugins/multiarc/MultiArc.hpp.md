# MultiArc.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `ArcCommand`: Iterates through collection and processes each element, storing results
- `ArcItemUserData`: Iterates through collection and processes each element, storing results
- `ArcPlugins`: Iterates through collection and processes each element, storing results
- `InitDialogItem`: Iterates through collection and processes each element, storing results
- `KillStruct`: Iterates through collection and processes each element, storing results
- `MAAdvFlags`: Iterates through collection and processes each element, storing results
- `Options`: Iterates through collection and processes each element, storing results
- `PluginClass`: Iterates through collection and processes each element, storing results
- `PluginItem`: Iterates through collection and processes each element, storing results
### Key Functions
- `IsArchive()`: Iterates through collection and processes each element, storing results
- `OpenArchive()`: Iterates through collection and processes each element, storing results
- `GetArcItem()`: Iterates through collection and processes each element, storing results
- `CloseArchive()`: Iterates through collection and processes each element, storing results
- `GetFormatName()`: Iterates through collection and processes each element, storing results
- `GetDefaultCommands()`: Iterates through collection and processes each element, storing results
- `FmtCount()`: Iterates through collection and processes each element, storing results
- `LoadFmtModules()`: Iterates through collection and processes each element, storing results
- `CompareFmtModules()`: Iterates through collection and processes each element, storing results
- `GetGroupName()`: Iterates through collection and processes each element, storing results
### Summary
The `MultiArc.hpp` file provides essential functionality for core functionality. It defines 9 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `ArcCommand`: Iterates through collection and processes each element, storing results
- `ArcItemUserData`: Iterates through collection and processes each element, storing results
- `ArcPlugins`: Iterates through collection and processes each element, storing results
- `InitDialogItem`: Iterates through collection and processes each element, storing results
- `KillStruct`: Iterates through collection and processes each element, storing results
- `MAAdvFlags`: Iterates through collection and processes each element, storing results
- `Options`: Iterates through collection and processes each element, storing results
- `PluginClass`: Iterates through collection and processes each element, storing results
- `PluginItem`: Iterates through collection and processes each element, storing results
### Kluczowe Funkcje
- `IsArchive()`: Iterates through collection and processes each element, storing results
- `OpenArchive()`: Iterates through collection and processes each element, storing results
- `GetArcItem()`: Iterates through collection and processes each element, storing results
- `CloseArchive()`: Iterates through collection and processes each element, storing results
- `GetFormatName()`: Iterates through collection and processes each element, storing results
- `GetDefaultCommands()`: Iterates through collection and processes each element, storing results
- `FmtCount()`: Iterates through collection and processes each element, storing results
- `LoadFmtModules()`: Iterates through collection and processes each element, storing results
- `CompareFmtModules()`: Iterates through collection and processes each element, storing results
- `GetGroupName()`: Iterates through collection and processes each element, storing results
### Podsumowanie
Plik `MultiArc.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 9 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
