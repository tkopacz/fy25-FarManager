# Rtl.Memory.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `HeapAlloc()`: Performs HeapAlloc operation as part of the component's functionality
- `HeapReAlloc()`: Performs HeapReAlloc operation as part of the component's functionality
- `free()`: Performs free operation as part of the component's functionality
- `new()`: Allocates memory and initializes resources for new
- `malloc()`: Allocates memory and initializes resources for malloc
- `delete()`: Removes item from collection and frees associated resources
- `__cxa_pure_virtual()`: Performs __cxa_pure_virtual operation as part of the component's functionality
- `RtlFreeMemory()`: Performs RtlFreeMemory operation as part of the component's functionality
- `realloc()`: Performs realloc operation as part of the component's functionality
- `RtlAllocateMemory()`: Allocates memory and initializes resources for RtlAllocateMemory
### Summary
The `Rtl.Memory.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `HeapAlloc()`: Performs HeapAlloc operation as part of the component's functionality
- `HeapReAlloc()`: Performs HeapReAlloc operation as part of the component's functionality
- `free()`: Performs free operation as part of the component's functionality
- `new()`: Allocates memory and initializes resources for new
- `malloc()`: Allocates memory and initializes resources for malloc
- `delete()`: Usuwa element z kolekcji i zwalnia powiązane zasoby
- `__cxa_pure_virtual()`: Performs __cxa_pure_virtual operation as part of the component's functionality
- `RtlFreeMemory()`: Performs RtlFreeMemory operation as part of the component's functionality
- `realloc()`: Performs realloc operation as part of the component's functionality
- `RtlAllocateMemory()`: Allocates memory and initializes resources for RtlAllocateMemory
### Podsumowanie
Plik `Rtl.Memory.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
