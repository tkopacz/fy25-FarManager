# filefilterparams.cpp
## English
### Purpose
This file implements functionality related to: Параметры Файлового фильтра
Main functional areas: file system operations
### Key Classes
- `attribute_map`: Implements attribute_map functionality
- `ffp_context`: Implements ffp_context functionality
- `filter_file_object`: Implements filter_file_object functionality
### Key Functions
- `bool()`: Implements bool functionality
- `FileInFilter()`: Implements FileInFilter functionality
- `AttributesString()`: Да! Файл выдержал все испытания и будет допущен к использованию в вызвавшей эту функцию операции
- `pad_right()`: Implements pad_right functionality
- `MenuString()`: Implements MenuString functionality
- `HighlightDlgUpdateUserControl()`: Implements HighlightDlgUpdateUserControl functionality
- `FilterDlgRelativeDateItemsUpdate()`: Implements FilterDlgRelativeDateItemsUpdate functionality
- `AttributesDialog()`: Processes input by dispatching to different code paths based on type or value
- `Builder()`: Processes input by dispatching to different code paths based on type or value
- `FileFilterConfigDlgProc()`: Processes input by dispatching to different code paths based on type or value
### Summary
The `filefilterparams.cpp` file provides essential functionality for file system operations. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Параметры Файлового фильтра
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Klasy
- `attribute_map`: Implements attribute_map functionality
- `ffp_context`: Implements ffp_context functionality
- `filter_file_object`: Implements filter_file_object functionality
### Kluczowe Funkcje
- `bool()`: Implements bool functionality
- `FileInFilter()`: Implements FileInFilter functionality
- `AttributesString()`: Да! Файл выдержал все испытания и будет допущен к использованию в вызвавшей эту функцию операции
- `pad_right()`: Implements pad_right functionality
- `MenuString()`: Implements MenuString functionality
- `HighlightDlgUpdateUserControl()`: Implements HighlightDlgUpdateUserControl functionality
- `FilterDlgRelativeDateItemsUpdate()`: Implements FilterDlgRelativeDateItemsUpdate functionality
- `AttributesDialog()`: Processes input by dispatching to different code paths based on type or value
- `Builder()`: Processes input by dispatching to different code paths based on type or value
- `FileFilterConfigDlgProc()`: Processes input by dispatching to different code paths based on type or value
### Podsumowanie
Plik `filefilterparams.cpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
