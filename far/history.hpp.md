# history.hpp
## English
### Purpose
This file implements functionality related to: История (Alt-F8, Alt-F11, Alt-F12)
Main functional areas: core functionality
### Key Classes
- `Bool3Option`: Performs Bool3Option operation as part of the component's functionality
- `Dialog`: Performs Dialog operation as part of the component's functionality
- `History`: Performs History operation as part of the component's functionality
- `HistoryConfig`: Performs HistoryConfig operation as part of the component's functionality
- `VMenu2`: Performs VMenu2 operation as part of the component's functionality
### Key Functions
- `AddToHistory()`: Adds new ToHistory to collection or list
- `Select()`: Performs Select operation as part of the component's functionality
- `GetPrev()`: Retrieves Prev from internal state or data structure
- `GetNext()`: Retrieves Next from internal state or data structure
- `GetSimilar()`: Retrieves Similar from internal state or data structure
- `ResetPosition()`: Performs ResetPosition operation as part of the component's functionality
- `DeleteIfUnlocked()`: Removes IfUnlocked from collection and frees associated resources
- `LastItem()`: Performs LastItem operation as part of the component's functionality
- `IsOnTop()`: Tests whether OnTop condition is true or property exists
- `CompactHistory()`: Performs CompactHistory operation as part of the component's functionality
### Summary
The `history.hpp` file provides essential functionality for core functionality. It defines 5 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: История (Alt-F8, Alt-F11, Alt-F12)
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `Bool3Option`: Performs Bool3Option operation as part of the component's functionality
- `Dialog`: Performs Dialog operation as part of the component's functionality
- `History`: Performs History operation as part of the component's functionality
- `HistoryConfig`: Performs HistoryConfig operation as part of the component's functionality
- `VMenu2`: Performs VMenu2 operation as part of the component's functionality
### Kluczowe Funkcje
- `AddToHistory()`: Dodaje nowy ToHistory do kolekcji lub listy
- `Select()`: Performs Select operation as part of the component's functionality
- `GetPrev()`: Pobiera Prev ze stanu wewnętrznego lub struktury danych
- `GetNext()`: Pobiera Next ze stanu wewnętrznego lub struktury danych
- `GetSimilar()`: Pobiera Similar ze stanu wewnętrznego lub struktury danych
- `ResetPosition()`: Performs ResetPosition operation as part of the component's functionality
- `DeleteIfUnlocked()`: Usuwa IfUnlocked z kolekcji i zwalnia powiązane zasoby
- `LastItem()`: Performs LastItem operation as part of the component's functionality
- `IsOnTop()`: Testuje czy OnTop warunek jest prawdziwy lub właściwość istnieje
- `CompactHistory()`: Performs CompactHistory operation as part of the component's functionality
### Podsumowanie
Plik `history.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 5 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
