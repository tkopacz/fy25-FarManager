# string_utils.cpp
## English
### Purpose
This file implements functionality related to: Сравнение без учета регистра, преобразование регистра
Main functional areas: string utilities
### Key Functions
- `GetBlanks()`: Retrieves Blanks value in string_utils.cpp
- `GetEols()`: Retrieves Eols value in string_utils.cpp
- `is_alpha()`: Checks a condition in string_utils.cpp and returns the result
- `is_alphanumeric()`: Checks a condition in string_utils.cpp and returns the result
- `is_upper()`: Checks a condition in string_utils.cpp and returns the result
- `is_lower()`: Checks a condition in string_utils.cpp and returns the result
- `upper()`: Performs upper operation in string_utils.cpp
- `lower()`: Performs lower operation in string_utils.cpp
- `fold()`: Performs fold operation in string_utils.cpp
- `equal_icase()`: Performs equal_icase operation in string_utils.cpp
### Summary
The `string_utils.cpp` file provides essential functionality for string utilities. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Сравнение без учета регистра, преобразование регистра
Główne obszary funkcjonalne: narzędzia łańcuchowe
### Kluczowe Funkcje
- `GetBlanks()`: Retrieves Blanks value in string_utils.cpp
- `GetEols()`: Retrieves Eols value in string_utils.cpp
- `is_alpha()`: Checks a condition in string_utils.cpp and returns the result
- `is_alphanumeric()`: Checks a condition in string_utils.cpp and returns the result
- `is_upper()`: Checks a condition in string_utils.cpp and returns the result
- `is_lower()`: Checks a condition in string_utils.cpp and returns the result
- `upper()`: Performs upper operation in string_utils.cpp
- `lower()`: Performs lower operation in string_utils.cpp
- `fold()`: Performs fold operation in string_utils.cpp
- `equal_icase()`: Performs equal_icase operation in string_utils.cpp
### Podsumowanie
Plik `string_utils.cpp` zapewnia podstawową funkcjonalność dla narzędzia łańcuchowe. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
