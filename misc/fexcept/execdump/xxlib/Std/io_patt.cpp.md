# io_patt.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `CharInStr()`: Checks a condition in io_patt.cpp and returns true or false based on the result
- `InPattern()`: Checks a condition in io_patt.cpp and returns true or false based on the result
- `CmpFile()`: Checks a condition in io_patt.cpp and returns true or false based on the result
### Summary
The `io_patt.cpp` file provides essential functionality for core functionality. and contains approximately 3 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `CharInStr()`: Checks a condition in io_patt.cpp and returns true or false based on the result
- `InPattern()`: Checks a condition in io_patt.cpp and returns true or false based on the result
- `CmpFile()`: Checks a condition in io_patt.cpp and returns true or false based on the result
### Podsumowanie
Plik `io_patt.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 3 funkcji wspierających operacje menedżera plików Far Manager.
