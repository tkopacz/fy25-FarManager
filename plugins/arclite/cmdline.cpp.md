# cmdline.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `Param`: Performs Param operation as part of the component's functionality
### Key Functions
- `lc()`: Performs lc operation as part of the component's functionality
- `parse_command()`: Parses input string or data into structured format
- `parse_plugin_call()`: Parses input string or data into structured format
- `is_param()`: Tests whether _param condition is true or property exists
- `parse_param()`: Parses input string or data into structured format
- `parse_bool_value()`: Parses input string or data into structured format
- `FAIL()`: Performs FAIL operation as part of the component's functionality
- `parse_tri_state_value()`: Parses input string or data into structured format
- `parse_open_command()`: Parses input string or data into structured format
- `parse_update_command()`: Parses input string or data into structured format
### Summary
The `cmdline.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `Param`: Performs Param operation as part of the component's functionality
### Kluczowe Funkcje
- `lc()`: Performs lc operation as part of the component's functionality
- `parse_command()`: Parsuje łańcuch wejściowy lub dane do formatu strukturalnego
- `parse_plugin_call()`: Parsuje łańcuch wejściowy lub dane do formatu strukturalnego
- `is_param()`: Testuje czy _param warunek jest prawdziwy lub właściwość istnieje
- `parse_param()`: Parsuje łańcuch wejściowy lub dane do formatu strukturalnego
- `parse_bool_value()`: Parsuje łańcuch wejściowy lub dane do formatu strukturalnego
- `FAIL()`: Performs FAIL operation as part of the component's functionality
- `parse_tri_state_value()`: Parsuje łańcuch wejściowy lub dane do formatu strukturalnego
- `parse_open_command()`: Parsuje łańcuch wejściowy lub dane do formatu strukturalnego
- `parse_update_command()`: Parsuje łańcuch wejściowy lub dane do formatu strukturalnego
### Podsumowanie
Plik `cmdline.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
