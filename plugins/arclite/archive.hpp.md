# archive.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `ArcAPI`: Performs ArcAPI operation as part of the component's functionality
- `ArcChain`: Performs ArcChain operation as part of the component's functionality
- `ArcEntry`: Performs ArcEntry operation as part of the component's functionality
- `ArcFileInfo`: Performs ArcFileInfo operation as part of the component's functionality
- `ArcFormat`: Performs ArcFormat operation as part of the component's functionality
- `ArcFormats`: Performs ArcFormats operation as part of the component's functionality
- `ArcLib`: Performs ArcLib operation as part of the component's functionality
- `Archive`: Performs Archive operation as part of the component's functionality
- `CDllCodecInfo`: Performs CDllCodecInfo operation as part of the component's functionality
- `CDllHasherInfo`: Performs CDllHasherInfo operation as part of the component's functionality
### Key Functions
- `get_prop()`: Retrieves _prop from internal state or data structure
- `get_bool_prop()`: Retrieves _bool_prop from internal state or data structure
- `get_uint_prop()`: Retrieves _uint_prop from internal state or data structure
- `get_string_prop()`: Retrieves _string_prop from internal state or data structure
- `get_bytes_prop()`: Retrieves _bytes_prop from internal state or data structure
- `default_extension()`: Performs default_extension operation as part of the component's functionality
- `Flags_KeepName()`: Evaluates conditions and returns boolean indicating success or validity
- `Flags_FindSignature()`: Evaluates conditions and returns boolean indicating success or validity
- `Flags_AltStreams()`: Evaluates conditions and returns boolean indicating success or validity
- `Flags_NtSecure()`: Evaluates conditions and returns boolean indicating success or validity
### Summary
The `archive.hpp` file provides essential functionality for core functionality. It defines 14 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `ArcAPI`: Performs ArcAPI operation as part of the component's functionality
- `ArcChain`: Performs ArcChain operation as part of the component's functionality
- `ArcEntry`: Performs ArcEntry operation as part of the component's functionality
- `ArcFileInfo`: Performs ArcFileInfo operation as part of the component's functionality
- `ArcFormat`: Performs ArcFormat operation as part of the component's functionality
- `ArcFormats`: Performs ArcFormats operation as part of the component's functionality
- `ArcLib`: Performs ArcLib operation as part of the component's functionality
- `Archive`: Performs Archive operation as part of the component's functionality
- `CDllCodecInfo`: Performs CDllCodecInfo operation as part of the component's functionality
- `CDllHasherInfo`: Performs CDllHasherInfo operation as part of the component's functionality
### Kluczowe Funkcje
- `get_prop()`: Pobiera _prop ze stanu wewnętrznego lub struktury danych
- `get_bool_prop()`: Pobiera _bool_prop ze stanu wewnętrznego lub struktury danych
- `get_uint_prop()`: Pobiera _uint_prop ze stanu wewnętrznego lub struktury danych
- `get_string_prop()`: Pobiera _string_prop ze stanu wewnętrznego lub struktury danych
- `get_bytes_prop()`: Pobiera _bytes_prop ze stanu wewnętrznego lub struktury danych
- `default_extension()`: Performs default_extension operation as part of the component's functionality
- `Flags_KeepName()`: Evaluates conditions and returns boolean indicating success or validity
- `Flags_FindSignature()`: Evaluates conditions and returns boolean indicating success or validity
- `Flags_AltStreams()`: Evaluates conditions and returns boolean indicating success or validity
- `Flags_NtSecure()`: Evaluates conditions and returns boolean indicating success or validity
### Podsumowanie
Plik `archive.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 14 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
