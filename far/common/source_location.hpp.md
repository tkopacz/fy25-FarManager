# source_location.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `source_location`: If we have our own, we can make it a bit saner as well column() is: - handled differently by MSVC and Clang - not supported at all by GCC
### Key Functions
- `file_name()`: column() is: - handled differently by MSVC and Clang - not supported at all by GCC - useless in general so not implemented
- `function_name()`: Implements function_name functionality
- `line()`: column() is: - handled differently by MSVC and Clang - not supported at all by GCC - useless in general so not implemented
### Summary
The `source_location.hpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 3 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `source_location`: If we have our own, we can make it a bit saner as well column() is: - handled differently by MSVC and Clang - not supported at all by GCC
### Kluczowe Funkcje
- `file_name()`: column() is: - handled differently by MSVC and Clang - not supported at all by GCC - useless in general so not implemented
- `function_name()`: Implements function_name functionality
- `line()`: column() is: - handled differently by MSVC and Clang - not supported at all by GCC - useless in general so not implemented
### Podsumowanie
Plik `source_location.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 3 funkcji wspierających operacje menedżera plików Far Manager.
