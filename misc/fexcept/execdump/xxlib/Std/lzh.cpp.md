# lzh.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `LZHMemCompress`: Performs LZHMemCompress operation as part of the component's functionality
### Key Functions
- `InitTree()`: Initializes data structures and sets up initial state for operation
- `InsertNode()`: Performs InsertNode operation as part of the component's functionality
- `DeleteNode()`: Removes Node from collection and frees associated resources
- `GetBit()`: Retrieves Bit from internal state or data structure
- `GetByte()`: Retrieves Byte from internal state or data structure
- `Putcode()`: Performs Putcode operation as part of the component's functionality
- `StartHuff()`: Performs StartHuff operation as part of the component's functionality
- `reconst()`: Performs reconst operation as part of the component's functionality
- `update()`: Updates existing data with new values while preserving structure
- `EncodeChar()`: Evaluates conditions and returns a boolean indicating success or validity of EncodeChar
### Summary
The `lzh.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `LZHMemCompress`: Performs LZHMemCompress operation as part of the component's functionality
### Kluczowe Funkcje
- `InitTree()`: Inicjalizuje struktury danych i ustawia stan początkowy dla operacji
- `InsertNode()`: Performs InsertNode operation as part of the component's functionality
- `DeleteNode()`: Usuwa Node z kolekcji i zwalnia powiązane zasoby
- `GetBit()`: Pobiera Bit ze stanu wewnętrznego lub struktury danych
- `GetByte()`: Pobiera Byte ze stanu wewnętrznego lub struktury danych
- `Putcode()`: Performs Putcode operation as part of the component's functionality
- `StartHuff()`: Performs StartHuff operation as part of the component's functionality
- `reconst()`: Performs reconst operation as part of the component's functionality
- `update()`: Aktualizuje istniejące dane nowymi wartościami zachowując strukturę
- `EncodeChar()`: Evaluates conditions and returns a boolean indicating success or validity of EncodeChar
### Podsumowanie
Plik `lzh.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
