# FtpAPI.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `ParseDirLine()`: Parses input string or data into structured format
- `FtpKeepAlive()`: Evaluates conditions and returns a boolean indicating success or validity of FtpKeepAlive
- `FtpIsResume()`: Evaluates conditions and returns a boolean indicating success or validity of FtpIsResume
- `FtpCmdLineAlive()`: Evaluates conditions and returns boolean indicating success or validity
- `FtpSetRetryCount()`: Performs FtpSetRetryCount operation as part of the component's functionality
- `FtpSetBreakable()`: Evaluates conditions and returns a boolean indicating success or validity of FtpSetBreakable
- `FtpGetRetryCount()`: Performs FtpGetRetryCount operation as part of the component's functionality
- `FtpConnectMessage()`: Performs FtpConnectMessage operation as part of the component's functionality
- `FtpCmdBlock()`: Performs FtpCmdBlock operation as part of the component's functionality
- `FtpFindFirstFile()`: Evaluates conditions and returns a boolean indicating success or validity of FtpFindFirstFile
### Summary
The `FtpAPI.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `ParseDirLine()`: Parsuje łańcuch wejściowy lub dane do formatu strukturalnego
- `FtpKeepAlive()`: Evaluates conditions and returns a boolean indicating success or validity of FtpKeepAlive
- `FtpIsResume()`: Evaluates conditions and returns a boolean indicating success or validity of FtpIsResume
- `FtpCmdLineAlive()`: Evaluates conditions and returns boolean indicating success or validity
- `FtpSetRetryCount()`: Performs FtpSetRetryCount operation as part of the component's functionality
- `FtpSetBreakable()`: Evaluates conditions and returns a boolean indicating success or validity of FtpSetBreakable
- `FtpGetRetryCount()`: Performs FtpGetRetryCount operation as part of the component's functionality
- `FtpConnectMessage()`: Performs FtpConnectMessage operation as part of the component's functionality
- `FtpCmdBlock()`: Performs FtpCmdBlock operation as part of the component's functionality
- `FtpFindFirstFile()`: Evaluates conditions and returns a boolean indicating success or validity of FtpFindFirstFile
### Podsumowanie
Plik `FtpAPI.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
