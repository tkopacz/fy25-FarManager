# panelmix.cpp
## English
### Purpose
This file implements functionality related to: Commonly used panel related functions
Main functional areas: panel management
### Key Classes
- `column_info`: Implements column_info functionality
### Key Functions
- `ShellUpdatePanels()`: Processes input by dispatching to different code paths based on type or value
- `CheckUpdateAnotherPanel()`: Implements CheckUpdateAnotherPanel functionality
- `MakePath()`: учтем короткость имен :-)
- `MakePathForUI()`: Вставить сетевое (UNC) путь из левой панели Вставить сетевое (UNC) путь из правой панели Вставить сетевое (UNC) путь из активной панели
- `EnumWidths()`: Implements EnumWidths functionality
- `EnumWidthsRange()`: Implements EnumWidthsRange functionality
- `FormatStr_Attribute()`: Iterates through collection and processes each element, storing results
- `FormatStr_DateTime()`: Iterates through collection and processes each element, storing results
- `fit_to_right()`: Processes input by dispatching to different code paths based on type or value
- `FormatStr_Size()`: Implements FormatStr_Size functionality
### Summary
The `panelmix.cpp` file provides essential functionality for panel management. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Commonly used panel related functions
Główne obszary funkcjonalne: zarządzanie panelami
### Kluczowe Klasy
- `column_info`: Implements column_info functionality
### Kluczowe Funkcje
- `ShellUpdatePanels()`: Processes input by dispatching to different code paths based on type or value
- `CheckUpdateAnotherPanel()`: Implements CheckUpdateAnotherPanel functionality
- `MakePath()`: учтем короткость имен :-)
- `MakePathForUI()`: Вставить сетевое (UNC) путь из левой панели Вставить сетевое (UNC) путь из правой панели Вставить сетевое (UNC) путь из активной панели
- `EnumWidths()`: Implements EnumWidths functionality
- `EnumWidthsRange()`: Implements EnumWidthsRange functionality
- `FormatStr_Attribute()`: Iterates through collection and processes each element, storing results
- `FormatStr_DateTime()`: Iterates through collection and processes each element, storing results
- `fit_to_right()`: Processes input by dispatching to different code paths based on type or value
- `FormatStr_Size()`: Implements FormatStr_Size functionality
### Podsumowanie
Plik `panelmix.cpp` zapewnia podstawową funkcjonalność dla zarządzanie panelami. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
