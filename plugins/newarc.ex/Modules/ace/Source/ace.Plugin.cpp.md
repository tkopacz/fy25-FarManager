# ace.Plugin.cpp
## English
### Purpose
Main functional areas: plugin interface
### Key Classes
- `ACEHEADER`: Performs ACEHEADER operation in ace.Plugin.cpp
### Key Functions
- `IsACE()`: Checks a condition in ace.Plugin.cpp and returns true or false based on the result
- `AceArchive()`: (for instance: list DIR1\TEST.DAT
- `__CreateFileList()`: Opens a file or creates a file handle in ace.Plugin.cpp
- `free()`: Checks a condition in ace.Plugin.cpp and returns true or false based on the result
- `message()`: Performs message operation in ace.Plugin.cpp
### Summary
The `ace.Plugin.cpp` file provides essential functionality for plugin interface. It defines 1 class(es) and contains approximately 5 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: interfejs wtyczek
### Kluczowe Klasy
- `ACEHEADER`: Performs ACEHEADER operation in ace.Plugin.cpp
### Kluczowe Funkcje
- `IsACE()`: Checks a condition in ace.Plugin.cpp and returns true or false based on the result
- `AceArchive()`: (for instance: list DIR1\TEST.DAT
- `__CreateFileList()`: Opens a file or creates a file handle in ace.Plugin.cpp
- `free()`: Checks a condition in ace.Plugin.cpp and returns true or false based on the result
- `message()`: Performs message operation in ace.Plugin.cpp
### Podsumowanie
Plik `ace.Plugin.cpp` zapewnia podstawową funkcjonalność dla interfejs wtyczek. Definiuje 1 klas(y) i zawiera około 5 funkcji wspierających operacje menedżera plików Far Manager.
