# platform.cpp
## English
### Purpose
This file implements functionality related to: Враперы вокруг некоторых WinAPI функций
Main functional areas: core functionality
### Key Classes
- `format_message_error_context`: Performs format_message_error_context operation in platform.cpp
- `language_item`: Performs language_item operation in platform.cpp
- `layout_item`: Performs layout_item operation in platform.cpp
### Key Functions
- `ApiDynamicStringReceiverImpl()`: Performs ApiDynamicStringReceiverImpl operation in platform.cpp
- `single_wait()`: Performs single_wait operation in platform.cpp
- `multi_wait()`: Validates a condition in platform.cpp and throws an exception if the validation fails
- `set_error_mode()`: Sets or updates _error_mode value in platform.cpp
- `unset_error_mode()`: Performs unset_error_mode operation in platform.cpp
- `get_last_nt_status()`: Retrieves _last_nt_status value in platform.cpp
- `get_last_error()`: Retrieves _last_error value in platform.cpp
- `set_last_nt_status()`: Iterates through elements in platform.cpp and adds them to a collection
- `set_last_error_from_ntstatus()`: Iterates through elements in platform.cpp and adds them to a collection
- `postprocess_error_string()`: Iterates through elements in platform.cpp and adds them to a collection
### Namespaces
- `detail`
- `os`
- `rtdl`
- `uuid`
### Summary
The `platform.cpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Враперы вокруг некоторых WinAPI функций
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `format_message_error_context`: Performs format_message_error_context operation in platform.cpp
- `language_item`: Performs language_item operation in platform.cpp
- `layout_item`: Performs layout_item operation in platform.cpp
### Kluczowe Funkcje
- `ApiDynamicStringReceiverImpl()`: Performs ApiDynamicStringReceiverImpl operation in platform.cpp
- `single_wait()`: Performs single_wait operation in platform.cpp
- `multi_wait()`: Validates a condition in platform.cpp and throws an exception if the validation fails
- `set_error_mode()`: Sets or updates _error_mode value in platform.cpp
- `unset_error_mode()`: Performs unset_error_mode operation in platform.cpp
- `get_last_nt_status()`: Retrieves _last_nt_status value in platform.cpp
- `get_last_error()`: Retrieves _last_error value in platform.cpp
- `set_last_nt_status()`: Iterates through elements in platform.cpp and adds them to a collection
- `set_last_error_from_ntstatus()`: Iterates through elements in platform.cpp and adds them to a collection
- `postprocess_error_string()`: Iterates through elements in platform.cpp and adds them to a collection
### Przestrzenie nazw
- `detail`
- `os`
- `rtdl`
- `uuid`
### Podsumowanie
Plik `platform.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
