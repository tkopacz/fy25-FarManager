# filetype.cpp
## English
### Purpose
This file implements functionality related to: Работа с ассоциациями файлов
Main functional areas: file system operations
### Key Classes
- `data_item`: Performs data_item operation in filetype.cpp
- `menu_data`: Performs menu_data operation in filetype.cpp
### Key Functions
- `Context()`: We have to set it - users can have associations like !.! which will work funny without this
- `GetFiletypeOpenMode()`: We've already processed them!
- `ProcessExternal()`: non-default key may be pressed, it's ok.
- `strExecStr()`: We have to set it - users can have associations like !.! which will work funny without this
- `FillFileTypesMenu()`: Performs FillFileTypesMenu operation in filetype.cpp
- `EditTypeRecordDlgProc()`: Performs EditTypeRecordDlgProc operation in filetype.cpp
- `EditTypeRecord()`: Checks a condition in filetype.cpp and returns true or false based on the result
- `DeleteTypeRecord()`: Checks a condition in filetype.cpp and returns true or false based on the result
- `EditFileTypes()`: Checks a condition in filetype.cpp and returns true or false based on the result
### Summary
The `filetype.cpp` file provides essential functionality for file system operations. It defines 2 class(es) and contains approximately 9 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Работа с ассоциациями файлов
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Klasy
- `data_item`: Performs data_item operation in filetype.cpp
- `menu_data`: Performs menu_data operation in filetype.cpp
### Kluczowe Funkcje
- `Context()`: We have to set it - users can have associations like !.! which will work funny without this
- `GetFiletypeOpenMode()`: We've already processed them!
- `ProcessExternal()`: non-default key may be pressed, it's ok.
- `strExecStr()`: We have to set it - users can have associations like !.! which will work funny without this
- `FillFileTypesMenu()`: Performs FillFileTypesMenu operation in filetype.cpp
- `EditTypeRecordDlgProc()`: Performs EditTypeRecordDlgProc operation in filetype.cpp
- `EditTypeRecord()`: Checks a condition in filetype.cpp and returns true or false based on the result
- `DeleteTypeRecord()`: Checks a condition in filetype.cpp and returns true or false based on the result
- `EditFileTypes()`: Checks a condition in filetype.cpp and returns true or false based on the result
### Podsumowanie
Plik `filetype.cpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. Definiuje 2 klas(y) i zawiera około 9 funkcji wspierających operacje menedżera plików Far Manager.
