# io_info.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `GetStartupDir()`: argv[0] has path HOME variable @brief Ge startup dir for specified module Function returns path from where module start
- `AddLastSlash()`: @brief Locate path for temprorary directory
- `GetTmpDir()`: argv[0] has path HOME variable @brief Ge startup dir for specified module Function returns path from where module start
- `GetCurDir()`: Retrieves CurDir from current context or object state
- `LocalFile()`: argv[0] has path HOME variable @brief Ge startup dir for specified module Function returns path from where module start
- `QueryFileType()`: argv[0] has path HOME variable @brief Ge startup dir for specified module Function returns path from where module start
### Summary
The `io_info.cpp` file provides essential functionality for core functionality. and contains approximately 6 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `GetStartupDir()`: argv[0] has path HOME variable @brief Ge startup dir for specified module Function returns path from where module start
- `AddLastSlash()`: @brief Locate path for temprorary directory
- `GetTmpDir()`: argv[0] has path HOME variable @brief Ge startup dir for specified module Function returns path from where module start
- `GetCurDir()`: Retrieves CurDir from current context or object state
- `LocalFile()`: argv[0] has path HOME variable @brief Ge startup dir for specified module Function returns path from where module start
- `QueryFileType()`: argv[0] has path HOME variable @brief Ge startup dir for specified module Function returns path from where module start
### Podsumowanie
Plik `io_info.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 6 funkcji wspierających operacje menedżera plików Far Manager.
