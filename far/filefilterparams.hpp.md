# filefilterparams.hpp
## English
### Purpose
This file implements functionality related to: Параметры Файлового фильтра
Main functional areas: file system operations
### Key Classes
- `FileFilterParams`: Implements FileFilterParams functionality
- `filter_area`: Implements filter_area functionality
- `filter_dates`: Implements filter_dates functionality
- `fmask`: Implements fmask functionality
### Key Functions
- `filter_dates()`: Implements filter_dates functionality
- `bool()`: Implements bool functionality
- `Clone()`: Implements Clone functionality
- `SetTitle()`: Updates Title with provided value and validates constraints
- `SetMask()`: Updates Mask with provided value and validates constraints
- `SetDate()`: Updates Date with provided value and validates constraints
- `SetSize()`: Updates Size with provided value and validates constraints
- `SetHardLinks()`: Updates HardLinks with provided value and validates constraints
- `SetAttr()`: Updates Attr with provided value and validates constraints
- `SetColors()`: Updates Colors with provided value and validates constraints
### Summary
The `filefilterparams.hpp` file provides essential functionality for file system operations. It defines 4 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Параметры Файлового фильтра
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Klasy
- `FileFilterParams`: Implements FileFilterParams functionality
- `filter_area`: Implements filter_area functionality
- `filter_dates`: Implements filter_dates functionality
- `fmask`: Implements fmask functionality
### Kluczowe Funkcje
- `filter_dates()`: Implements filter_dates functionality
- `bool()`: Implements bool functionality
- `Clone()`: Implements Clone functionality
- `SetTitle()`: Updates Title with provided value and validates constraints
- `SetMask()`: Updates Mask with provided value and validates constraints
- `SetDate()`: Updates Date with provided value and validates constraints
- `SetSize()`: Updates Size with provided value and validates constraints
- `SetHardLinks()`: Updates HardLinks with provided value and validates constraints
- `SetAttr()`: Updates Attr with provided value and validates constraints
- `SetColors()`: Updates Colors with provided value and validates constraints
### Podsumowanie
Plik `filefilterparams.hpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. Definiuje 4 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
