# hotplug.cpp
## English
### Purpose
This file implements functionality related to: Отключение Hotplug-устройств
Main functional areas: core functionality
### Key Classes
- `DeviceInfo`: Performs DeviceInfo operation in hotplug.cpp
- `device_paths`: Performs device_paths operation in hotplug.cpp
- `devinfo_handle_closer`: Performs devinfo_handle_closer operation in hotplug.cpp
### Key Functions
- `get()`: Checks a condition in hotplug.cpp and returns true or false based on the result
- `dev_info()`: Checks a condition in hotplug.cpp and returns true or false based on the result
- `bool()`: Performs bool operation in hotplug.cpp
- `OpenDeviceInfo()`: Performs OpenDeviceInfo operation in hotplug.cpp
- `GetDeviceRegistryProperty()`: Retrieves DeviceRegistryProperty value in hotplug.cpp
- `EnumDeviceInterfaces()`: Performs EnumDeviceInterfaces operation in hotplug.cpp
- `DeviceInterfacesEnumerator()`: Performs DeviceInterfacesEnumerator operation in hotplug.cpp
- `GetDevicePath()`: Retrieves DevicePath value in hotplug.cpp
- `Info()`: Performs Info operation in hotplug.cpp
- `Receiver()`: Performs Receiver operation in hotplug.cpp
### Namespaces
- `detail`
### Summary
The `hotplug.cpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Отключение Hotplug-устройств
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `DeviceInfo`: Performs DeviceInfo operation in hotplug.cpp
- `device_paths`: Performs device_paths operation in hotplug.cpp
- `devinfo_handle_closer`: Performs devinfo_handle_closer operation in hotplug.cpp
### Kluczowe Funkcje
- `get()`: Checks a condition in hotplug.cpp and returns true or false based on the result
- `dev_info()`: Checks a condition in hotplug.cpp and returns true or false based on the result
- `bool()`: Performs bool operation in hotplug.cpp
- `OpenDeviceInfo()`: Performs OpenDeviceInfo operation in hotplug.cpp
- `GetDeviceRegistryProperty()`: Retrieves DeviceRegistryProperty value in hotplug.cpp
- `EnumDeviceInterfaces()`: Performs EnumDeviceInterfaces operation in hotplug.cpp
- `DeviceInterfacesEnumerator()`: Performs DeviceInterfacesEnumerator operation in hotplug.cpp
- `GetDevicePath()`: Retrieves DevicePath value in hotplug.cpp
- `Info()`: Performs Info operation in hotplug.cpp
- `Receiver()`: Performs Receiver operation in hotplug.cpp
### Przestrzenie nazw
- `detail`
### Podsumowanie
Plik `hotplug.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
