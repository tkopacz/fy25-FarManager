# Patch7zCP.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `patched`: Performs patched operation as part of the component's functionality
### Key Functions
- `GetOEMCP()`: Retrieves OEMCP from internal state or data structure
- `GetACP()`: Retrieves ACP from internal state or data structure
- `MultiByteToWideChar()`: Performs MultiByteToWideChar operation as part of the component's functionality
- `WideCharToMultiByte()`: Performs WideCharToMultiByte operation as part of the component's functionality
- `PinhFromImageBase()`: Performs PinhFromImageBase operation as part of the component's functionality
- `PFromRva()`: Performs PFromRva operation as part of the component's functionality
- `CountOfImports()`: Performs CountOfImports operation as part of the component's functionality
- `patch_IAT()`: Evaluates conditions and returns a boolean indicating success or validity of patch_IAT
- `patch_7z_dll()`: Evaluates conditions and returns a boolean indicating success or validity of patch_7z_dll
- `is_valid_cp()`: Tests whether _valid_cp condition is true or property exists
### Summary
The `Patch7zCP.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `patched`: Performs patched operation as part of the component's functionality
### Kluczowe Funkcje
- `GetOEMCP()`: Pobiera OEMCP ze stanu wewnętrznego lub struktury danych
- `GetACP()`: Pobiera ACP ze stanu wewnętrznego lub struktury danych
- `MultiByteToWideChar()`: Performs MultiByteToWideChar operation as part of the component's functionality
- `WideCharToMultiByte()`: Performs WideCharToMultiByte operation as part of the component's functionality
- `PinhFromImageBase()`: Performs PinhFromImageBase operation as part of the component's functionality
- `PFromRva()`: Performs PFromRva operation as part of the component's functionality
- `CountOfImports()`: Performs CountOfImports operation as part of the component's functionality
- `patch_IAT()`: Evaluates conditions and returns a boolean indicating success or validity of patch_IAT
- `patch_7z_dll()`: Evaluates conditions and returns a boolean indicating success or validity of patch_7z_dll
- `is_valid_cp()`: Testuje czy _valid_cp warunek jest prawdziwy lub właściwość istnieje
### Podsumowanie
Plik `Patch7zCP.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
