# elevation.cpp
## English
### Purpose
This file implements functionality related to: Elevation
Main functional areas: core functionality
### Key Classes
- `EAData`: Implements EAData functionality
- `callback_param`: Implements callback_param functionality
- `elevated`: Implements elevated functionality
- `security_attributes_wrapper`: Implements security_attributes_wrapper functionality
### Key Functions
- `CreateBackupRestorePrivilege()`: Creates and initializes new object or resource instance
- `WritePipe()`: Implements WritePipe functionality
- `ReadPipe()`: Implements ReadPipe functionality
- `set_attributes()`: Updates _attributes with provided value and validates constraints
- `set_descriptor()`: Updates _descriptor with provided value and validates constraints
- `PrivilegedHander()`: Something went really bad, it's better to stop any further attempts
- `ElevatedHandler()`: Something went really bad, it's better to stop any further attempts
- `TerminateChildProcess()`: Something went really bad, it's better to stop any further attempts
- `make_admin_sid()`: Something went really bad, it's better to stop any further attempts
- `make_explicit_admin_access()`: Implements make_explicit_admin_access functionality
### Summary
The `elevation.cpp` file provides essential functionality for core functionality. It defines 4 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Elevation
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `EAData`: Implements EAData functionality
- `callback_param`: Implements callback_param functionality
- `elevated`: Implements elevated functionality
- `security_attributes_wrapper`: Implements security_attributes_wrapper functionality
### Kluczowe Funkcje
- `CreateBackupRestorePrivilege()`: Creates and initializes new object or resource instance
- `WritePipe()`: Implements WritePipe functionality
- `ReadPipe()`: Implements ReadPipe functionality
- `set_attributes()`: Updates _attributes with provided value and validates constraints
- `set_descriptor()`: Updates _descriptor with provided value and validates constraints
- `PrivilegedHander()`: Something went really bad, it's better to stop any further attempts
- `ElevatedHandler()`: Something went really bad, it's better to stop any further attempts
- `TerminateChildProcess()`: Something went really bad, it's better to stop any further attempts
- `make_admin_sid()`: Something went really bad, it's better to stop any further attempts
- `make_explicit_admin_access()`: Implements make_explicit_admin_access functionality
### Podsumowanie
Plik `elevation.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 4 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
