# filefilterparams.hpp
## English
### Purpose
This file implements functionality related to: Параметры Файлового фильтра
Main functional areas: file system operations
### Key Classes
- `FileFilterParams`: Performs FileFilterParams operation as part of the component's functionality
- `filter_area`: Performs filter_area operation as part of the component's functionality
- `filter_dates`: Performs filter_dates operation as part of the component's functionality
- `fmask`: Performs fmask operation as part of the component's functionality
### Key Functions
- `filter_dates()`: Performs filter_dates operation as part of the component's functionality
- `bool()`: Performs bool operation as part of the component's functionality
- `Clone()`: Performs Clone operation as part of the component's functionality
- `SetTitle()`: Updates Title in internal state or configuration
- `SetMask()`: Updates Mask in internal state or configuration
- `SetDate()`: Updates Date in internal state or configuration
- `SetSize()`: Updates Size in internal state or configuration
- `SetHardLinks()`: Updates HardLinks in internal state or configuration
- `SetAttr()`: Updates Attr in internal state or configuration
- `SetColors()`: Updates Colors in internal state or configuration
### Summary
The `filefilterparams.hpp` file provides essential functionality for file system operations. It defines 4 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Параметры Файлового фильтра
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Klasy
- `FileFilterParams`: Implementuje operacje systemu plików
- `filter_area`: Implementuje operacje systemu plików
- `filter_dates`: Implementuje operacje systemu plików
- `fmask`: Implementuje operacje systemu plików
### Kluczowe Funkcje
- `filter_dates()`: Wykonuje filter_dates operację jako część operacje systemu plików
- `bool()`: Wykonuje bool operację jako część operacje systemu plików
- `Clone()`: Wykonuje Clone operację jako część operacje systemu plików
- `SetTitle()`: Aktualizuje Title w stanie wewnętrznym lub konfiguracji
- `SetMask()`: Aktualizuje Mask w stanie wewnętrznym lub konfiguracji
- `SetDate()`: Aktualizuje Date w stanie wewnętrznym lub konfiguracji
- `SetSize()`: Aktualizuje Size w stanie wewnętrznym lub konfiguracji
- `SetHardLinks()`: Aktualizuje HardLinks w stanie wewnętrznym lub konfiguracji
- `SetAttr()`: Aktualizuje Attr w stanie wewnętrznym lub konfiguracji
- `SetColors()`: Aktualizuje Colors w stanie wewnętrznym lub konfiguracji
### Podsumowanie
Plik `filefilterparams.hpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. Definiuje 4 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
