# attr.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `PropInfo`: Performs PropInfo operation as part of the component's functionality
### Key Functions
- `uint_to_hex_str()`: Performs uint_to_hex_str operation as part of the component's functionality
- `format_str_prop()`: Performs format_str_prop operation as part of the component's functionality
- `format_int_prop()`: Performs format_int_prop operation as part of the component's functionality
- `format_uint_prop()`: Performs format_uint_prop operation as part of the component's functionality
- `format_size_prop()`: Returns the current size or capacity of the container
- `format_filetime_prop()`: Performs format_filetime_prop operation as part of the component's functionality
- `date_time()`: Performs date_time operation as part of the component's functionality
- `format_crc_prop()`: Performs format_crc_prop operation as part of the component's functionality
- `format_posix_attrib_prop()`: Performs format_posix_attrib_prop operation as part of the component's functionality
- `format_attrib_prop()`: Performs format_attrib_prop operation as part of the component's functionality
### Summary
The `attr.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `PropInfo`: Performs PropInfo operation as part of the component's functionality
### Kluczowe Funkcje
- `uint_to_hex_str()`: Performs uint_to_hex_str operation as part of the component's functionality
- `format_str_prop()`: Performs format_str_prop operation as part of the component's functionality
- `format_int_prop()`: Performs format_int_prop operation as part of the component's functionality
- `format_uint_prop()`: Performs format_uint_prop operation as part of the component's functionality
- `format_size_prop()`: Returns the current size or capacity of the container
- `format_filetime_prop()`: Performs format_filetime_prop operation as part of the component's functionality
- `date_time()`: Performs date_time operation as part of the component's functionality
- `format_crc_prop()`: Performs format_crc_prop operation as part of the component's functionality
- `format_posix_attrib_prop()`: Performs format_posix_attrib_prop operation as part of the component's functionality
- `format_attrib_prop()`: Performs format_attrib_prop operation as part of the component's functionality
### Podsumowanie
Plik `attr.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
