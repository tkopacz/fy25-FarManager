# pathutils.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `long_path()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `long_path_norm()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `add_trailing_slash()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `del_trailing_slash()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `locate_path_root()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `extract_path_root()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `extract_file_name()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `extract_file_path()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `extract_file_ext()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `is_root_path()`: Iterates through elements in pathutils.cpp and adds them to a collection
### Summary
The `pathutils.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `long_path()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `long_path_norm()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `add_trailing_slash()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `del_trailing_slash()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `locate_path_root()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `extract_path_root()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `extract_file_name()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `extract_file_path()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `extract_file_ext()`: Iterates through elements in pathutils.cpp and adds them to a collection
- `is_root_path()`: Iterates through elements in pathutils.cpp and adds them to a collection
### Podsumowanie
Plik `pathutils.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
