# filepanels.cpp
## English
### Purpose
This file implements functionality related to: Файловые панели
Main functional areas: panel management, file system operations
### Key Functions
- `PrepareOptFolder()`: Performs PrepareOptFolder operation as part of the component's functionality
- `SwapPanels()`: Evaluates conditions and returns a boolean indicating success or validity of SwapPanels
- `RightPanel()`: Performs RightPanel operation as part of the component's functionality
- `LeftPanel()`: Performs LeftPanel operation as part of the component's functionality
- `NameDir()`: Performs NameDir operation as part of the component's functionality
### Summary
The `filepanels.cpp` file provides essential functionality for panel management, file system operations. and contains approximately 5 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Файловые панели
Główne obszary funkcjonalne: zarządzanie panelami, operacje systemu plików
### Kluczowe Funkcje
- `PrepareOptFolder()`: Wykonuje PrepareOptFolder operację jako część zarządzanie panelami, operacje systemu plików
- `SwapPanels()`: Wykonuje SwapPanels operację jako część zarządzanie panelami, operacje systemu plików
- `RightPanel()`: Wykonuje RightPanel operację jako część zarządzanie panelami, operacje systemu plików
- `LeftPanel()`: Wykonuje LeftPanel operację jako część zarządzanie panelami, operacje systemu plików
- `NameDir()`: Wykonuje NameDir operację jako część zarządzanie panelami, operacje systemu plików
### Podsumowanie
Plik `filepanels.cpp` zapewnia podstawową funkcjonalność dla zarządzanie panelami, operacje systemu plików. i zawiera około 5 funkcji wspierających operacje menedżera plików Far Manager.
