# common.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `Attr`: Performs Attr operation as part of the component's functionality
- `ExtractOptions`: Performs ExtractOptions operation as part of the component's functionality
- `OpenOptions`: Opens a resource or connection for use
- `ProfileOptions`: Performs ProfileOptions operation as part of the component's functionality
- `ProgressMonitor`: Performs ProgressMonitor operation as part of the component's functionality
- `ProgressSuspend`: Performs ProgressSuspend operation as part of the component's functionality
- `SfxInstallConfig`: Performs SfxInstallConfig operation as part of the component's functionality
- `SfxOptions`: Performs SfxOptions operation as part of the component's functionality
- `SfxVersionInfo`: Performs SfxVersionInfo operation as part of the component's functionality
- `UpdateOptions`: Updates the current state or data with new values
### Key Functions
- `update_time()`: Updates existing data with new values while preserving structure
- `discard_time()`: Performs discard_time operation as part of the component's functionality
- `display()`: Displays content or makes visual element visible on screen
- `is_single_key()`: Tests whether _single_key condition is true or property exists
- `handle_esc()`: Handles _esc by executing appropriate response logic
- `update_ui()`: Updates existing data with new values while preserving structure
- `clean()`: Performs clean operation as part of the component's functionality
- `time_elapsed()`: Performs time_elapsed operation as part of the component's functionality
- `ticks_per_sec()`: Performs ticks_per_sec operation as part of the component's functionality
- `retry_or_ignore_error()`: Performs retry_or_ignore_error operation as part of the component's functionality
### Summary
The `common.hpp` file provides essential functionality for core functionality. It defines 12 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `Attr`: Performs Attr operation as part of the component's functionality
- `ExtractOptions`: Performs ExtractOptions operation as part of the component's functionality
- `OpenOptions`: Opens a resource or connection for use
- `ProfileOptions`: Performs ProfileOptions operation as part of the component's functionality
- `ProgressMonitor`: Performs ProgressMonitor operation as part of the component's functionality
- `ProgressSuspend`: Performs ProgressSuspend operation as part of the component's functionality
- `SfxInstallConfig`: Performs SfxInstallConfig operation as part of the component's functionality
- `SfxOptions`: Performs SfxOptions operation as part of the component's functionality
- `SfxVersionInfo`: Performs SfxVersionInfo operation as part of the component's functionality
- `UpdateOptions`: Updates the current state or data with new values
### Kluczowe Funkcje
- `update_time()`: Aktualizuje istniejące dane nowymi wartościami zachowując strukturę
- `discard_time()`: Performs discard_time operation as part of the component's functionality
- `display()`: Wyświetla zawartość lub czyni element wizualny widocznym na ekranie
- `is_single_key()`: Testuje czy _single_key warunek jest prawdziwy lub właściwość istnieje
- `handle_esc()`: Obsługuje _esc wykonując odpowiednią logikę odpowiedzi
- `update_ui()`: Aktualizuje istniejące dane nowymi wartościami zachowując strukturę
- `clean()`: Performs clean operation as part of the component's functionality
- `time_elapsed()`: Performs time_elapsed operation as part of the component's functionality
- `ticks_per_sec()`: Performs ticks_per_sec operation as part of the component's functionality
- `retry_or_ignore_error()`: Performs retry_or_ignore_error operation as part of the component's functionality
### Podsumowanie
Plik `common.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 12 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
