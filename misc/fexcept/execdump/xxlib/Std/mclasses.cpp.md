# mclasses.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `MyStringSortProc()`: Performs MyStringSortProc operation as part of the component's functionality
- `MyStringSearchProc()`: Performs MyStringSearchProc operation as part of the component's functionality
- `MyStringIncSearchProc()`: Performs MyStringIncSearchProc operation as part of the component's functionality
- `MyStringSearchCharProc()`: Performs MyStringSearchCharProc operation as part of the component's functionality
- `MyStringIncSearchCharProc()`: Performs MyStringIncSearchCharProc operation as part of the component's functionality
- `s()`: Performs s operation as part of the component's functionality
- `Search()`: Searches for item in collection and returns position or reference
### Summary
The `mclasses.cpp` file provides essential functionality for core functionality. and contains approximately 7 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `MyStringSortProc()`: Performs MyStringSortProc operation as part of the component's functionality
- `MyStringSearchProc()`: Performs MyStringSearchProc operation as part of the component's functionality
- `MyStringIncSearchProc()`: Performs MyStringIncSearchProc operation as part of the component's functionality
- `MyStringSearchCharProc()`: Performs MyStringSearchCharProc operation as part of the component's functionality
- `MyStringIncSearchCharProc()`: Performs MyStringIncSearchCharProc operation as part of the component's functionality
- `s()`: Performs s operation as part of the component's functionality
- `Search()`: Wyszukuje element w kolekcji i zwraca pozycję lub referencję
### Podsumowanie
Plik `mclasses.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 7 funkcji wspierających operacje menedżera plików Far Manager.
