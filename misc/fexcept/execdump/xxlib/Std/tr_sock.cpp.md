# tr_sock.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `Destroy()`: Performs Destroy operation in tr_sock.cpp
- `Close()`: Performs Close operation in tr_sock.cpp
- `ISRead()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
- `ISWrite()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
- `DOSend()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
- `FlushCache()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
- `UseHandle()`: Performs UseHandle operation in tr_sock.cpp
- `ReleaseHandle()`: Performs ReleaseHandle operation in tr_sock.cpp
- `Cvt()`: Performs Cvt operation in tr_sock.cpp
- `MemMove()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
### Summary
The `tr_sock.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `Destroy()`: Performs Destroy operation in tr_sock.cpp
- `Close()`: Performs Close operation in tr_sock.cpp
- `ISRead()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
- `ISWrite()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
- `DOSend()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
- `FlushCache()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
- `UseHandle()`: Performs UseHandle operation in tr_sock.cpp
- `ReleaseHandle()`: Performs ReleaseHandle operation in tr_sock.cpp
- `Cvt()`: Performs Cvt operation in tr_sock.cpp
- `MemMove()`: Checks a condition in tr_sock.cpp and returns true or false based on the result
### Podsumowanie
Plik `tr_sock.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
