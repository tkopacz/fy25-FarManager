# hilight.cpp
## English
### Purpose
This file implements functionality related to: Files highlighting
Main functional areas: core functionality
### Key Functions
- `file_color()`: Performs file_color operation as part of the component's functionality
- `mark_color()`: Performs mark_color operation as part of the component's functionality
- `SetHighlighting()`: Updates Highlighting in internal state or configuration
- `LoadHighlight()`: Loads data or resources from storage into memory
- `ApplyBlackOnBlackColor()`: Performs ApplyBlackOnBlackColor operation as part of the component's functionality
- `ApplyBlackOnBlackColors()`: Performs ApplyBlackOnBlackColors operation as part of the component's functionality
- `ApplyColors()`: Performs ApplyColors operation as part of the component's functionality
- `merge()`: Performs merge operation as part of the component's functionality
- `inherit_color()`: Performs inherit_color operation as part of the component's functionality
- `HighlightDlgUpdateUserControl()`: Performs HighlightDlgUpdateUserControl operation as part of the component's functionality
### Namespaces
- `colors`
- `names`
### Summary
The `hilight.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Files highlighting
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `file_color()`: Performs file_color operation as part of the component's functionality
- `mark_color()`: Performs mark_color operation as part of the component's functionality
- `SetHighlighting()`: Aktualizuje Highlighting w stanie wewnętrznym lub konfiguracji
- `LoadHighlight()`: Ładuje dane lub zasoby z magazynu do pamięci
- `ApplyBlackOnBlackColor()`: Performs ApplyBlackOnBlackColor operation as part of the component's functionality
- `ApplyBlackOnBlackColors()`: Performs ApplyBlackOnBlackColors operation as part of the component's functionality
- `ApplyColors()`: Performs ApplyColors operation as part of the component's functionality
- `merge()`: Performs merge operation as part of the component's functionality
- `inherit_color()`: Performs inherit_color operation as part of the component's functionality
- `HighlightDlgUpdateUserControl()`: Performs HighlightDlgUpdateUserControl operation as part of the component's functionality
### Przestrzenie nazw
- `colors`
- `names`
### Podsumowanie
Plik `hilight.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
