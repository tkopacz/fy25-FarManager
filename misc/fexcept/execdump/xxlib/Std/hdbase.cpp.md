# hdbase.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `DupName()`: Evaluates conditions and returns a boolean indicating success or validity of DupName
- `HDBType2Str()`: Performs HDBType2Str operation as part of the component's functionality
- `HDBColWidth()`: Performs HDBColWidth operation as part of the component's functionality
- `HDBCreate()`: Performs HDBCreate operation as part of the component's functionality
- `HDBDestroy()`: Performs HDBDestroy operation as part of the component's functionality
- `HDBValue2Str()`: Performs HDBValue2Str operation as part of the component's functionality
- `HDBDestroyColumns()`: Performs HDBDestroyColumns operation as part of the component's functionality
- `HDBCreateColumns()`: Allocates memory and initializes resources for HDBCreateColumns
- `HDBFillColumns()`: Evaluates conditions and returns a boolean indicating success or validity of HDBFillColumns
- `HDBCharConvert()`: Performs HDBCharConvert operation as part of the component's functionality
### Summary
The `hdbase.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `DupName()`: Evaluates conditions and returns a boolean indicating success or validity of DupName
- `HDBType2Str()`: Performs HDBType2Str operation as part of the component's functionality
- `HDBColWidth()`: Performs HDBColWidth operation as part of the component's functionality
- `HDBCreate()`: Performs HDBCreate operation as part of the component's functionality
- `HDBDestroy()`: Performs HDBDestroy operation as part of the component's functionality
- `HDBValue2Str()`: Performs HDBValue2Str operation as part of the component's functionality
- `HDBDestroyColumns()`: Performs HDBDestroyColumns operation as part of the component's functionality
- `HDBCreateColumns()`: Allocates memory and initializes resources for HDBCreateColumns
- `HDBFillColumns()`: Evaluates conditions and returns a boolean indicating success or validity of HDBFillColumns
- `HDBCharConvert()`: Performs HDBCharConvert operation as part of the component's functionality
### Podsumowanie
Plik `hdbase.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
