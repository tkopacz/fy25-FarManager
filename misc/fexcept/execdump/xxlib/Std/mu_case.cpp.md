# mu_case.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `ToUpper()`: Implements ToUpper functionality
- `ToLower()`: Implements ToLower functionality
- `isLower()`: Checks condition and returns boolean indicating state
- `isUpper()`: Checks condition and returns boolean indicating state
- `StrLwr()`: Processes input by dispatching to different code paths based on type or value
- `StrUpr()`: Processes input by dispatching to different code paths based on type or value
- `isSpace()`: Processes input by dispatching to different code paths based on type or value
- `StrCase()`: Processes input by dispatching to different code paths based on type or value
- `memicmp()`: Implements memicmp functionality
- `BuffCmp()`: Implements BuffCmp functionality
### Summary
The `mu_case.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `ToUpper()`: Implements ToUpper functionality
- `ToLower()`: Implements ToLower functionality
- `isLower()`: Checks condition and returns boolean indicating state
- `isUpper()`: Checks condition and returns boolean indicating state
- `StrLwr()`: Processes input by dispatching to different code paths based on type or value
- `StrUpr()`: Processes input by dispatching to different code paths based on type or value
- `isSpace()`: Processes input by dispatching to different code paths based on type or value
- `StrCase()`: Processes input by dispatching to different code paths based on type or value
- `memicmp()`: Implements memicmp functionality
- `BuffCmp()`: Implements BuffCmp functionality
### Podsumowanie
Plik `mu_case.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
