# perfthread.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `IWbemServices`: Performs IWbemServices operation as part of the component's functionality
- `PerfLib`: Performs PerfLib operation as part of the component's functionality
- `PerfThread`: Performs PerfThread operation as part of the component's functionality
- `ProcessPerfData`: Processes input data and performs the required operations
- `WMIConnection`: Performs WMIConnection operation as part of the component's functionality
- `counter_type`: Performs counter_type operation as part of the component's functionality
- `counters`: Performs counters operation as part of the component's functionality
### Key Functions
- `bool()`: Evaluates conditions and returns boolean indicating success or validity
- `Connect()`: Performs Connect operation as part of the component's functionality
- `Disconnect()`: Performs Disconnect operation as part of the component's functionality
- `SetProcessPriority()`: Updates ProcessPriority in internal state or configuration
- `TerminateProcess()`: Performs TerminateProcess operation as part of the component's functionality
- `AttachDebuggerToProcess()`: Performs AttachDebuggerToProcess operation as part of the component's functionality
- `ExecMethod()`: Performs ExecMethod operation as part of the component's functionality
- `lock()`: Performs lock operation as part of the component's functionality
- `unlock()`: Performs unlock operation as part of the component's functionality
- `ProcessData()`: Processes Data through transformation or handling pipeline
### Summary
The `perfthread.hpp` file provides essential functionality for core functionality. It defines 7 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `IWbemServices`: Performs IWbemServices operation as part of the component's functionality
- `PerfLib`: Performs PerfLib operation as part of the component's functionality
- `PerfThread`: Performs PerfThread operation as part of the component's functionality
- `ProcessPerfData`: Processes input data and performs the required operations
- `WMIConnection`: Performs WMIConnection operation as part of the component's functionality
- `counter_type`: Performs counter_type operation as part of the component's functionality
- `counters`: Performs counters operation as part of the component's functionality
### Kluczowe Funkcje
- `bool()`: Evaluates conditions and returns boolean indicating success or validity
- `Connect()`: Performs Connect operation as part of the component's functionality
- `Disconnect()`: Performs Disconnect operation as part of the component's functionality
- `SetProcessPriority()`: Aktualizuje ProcessPriority w stanie wewnętrznym lub konfiguracji
- `TerminateProcess()`: Performs TerminateProcess operation as part of the component's functionality
- `AttachDebuggerToProcess()`: Performs AttachDebuggerToProcess operation as part of the component's functionality
- `ExecMethod()`: Performs ExecMethod operation as part of the component's functionality
- `lock()`: Performs lock operation as part of the component's functionality
- `unlock()`: Performs unlock operation as part of the component's functionality
- `ProcessData()`: Przetwarza Data przez potok transformacji lub obsługi
### Podsumowanie
Plik `perfthread.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 7 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
