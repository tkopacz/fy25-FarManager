# datetime.hpp
## English
### Purpose
This file implements functionality related to: Функции для работы с датой и временем
Main functional areas: core functionality
### Key Classes
- `mode`: Implements mode functionality
- `time_check`: Implements time_check functionality
### Key Functions
- `uint8_t()`: Iterates through collection and processes each element, storing results
- `uint16_t()`: Iterates through collection and processes each element, storing results
- `uint32_t()`: Iterates through collection and processes each element, storing results
- `parse_time()`: (date, time) FullYear:
- `ParseTimePoint()`: (date, time) FullYear:
- `ParseDuration()`: (date, time) FullYear:
- `duration_to_string_hms()`: Implements duration_to_string_hms functionality
- `duration_to_string_hr()`: Implements duration_to_string_hr functionality
- `MkStrFTime()`: Implements MkStrFTime functionality
- `time_check()`: Implements time_check functionality
### Summary
The `datetime.hpp` file provides essential functionality for core functionality. It defines 2 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Функции для работы с датой и временем
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `mode`: Implements mode functionality
- `time_check`: Implements time_check functionality
### Kluczowe Funkcje
- `uint8_t()`: Iterates through collection and processes each element, storing results
- `uint16_t()`: Iterates through collection and processes each element, storing results
- `uint32_t()`: Iterates through collection and processes each element, storing results
- `parse_time()`: (date, time) FullYear:
- `ParseTimePoint()`: (date, time) FullYear:
- `ParseDuration()`: (date, time) FullYear:
- `duration_to_string_hms()`: Implements duration_to_string_hms functionality
- `duration_to_string_hr()`: Implements duration_to_string_hr functionality
- `MkStrFTime()`: Implements MkStrFTime functionality
- `time_check()`: Implements time_check functionality
### Podsumowanie
Plik `datetime.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 2 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
