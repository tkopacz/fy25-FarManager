# hmdpool.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `DataArray`: Performs DataArray operation as part of the component's functionality
- `HMemDataPool1`: Performs HMemDataPool1 operation as part of the component's functionality
### Key Functions
- `Free()`: Performs Free operation as part of the component's functionality
- `Detach()`: Performs Detach operation as part of the component's functionality
- `SetData()`: Updates Data in internal state or configuration
- `Clear()`: Clears all data and resets to initial empty state
- `LocateData()`: Evaluates conditions and returns boolean indicating success or validity
- `CreateData()`: Creates and initializes new Data instance
- `DeleteData()`: Removes Data from collection and frees associated resources
- `GetData()`: Retrieves Data from internal state or data structure
- `GetDataSize()`: Retrieves DataSize from internal state or data structure
- `SaveItem()`: Saves current state or data to persistent storage
### Summary
The `hmdpool.cpp` file provides essential functionality for core functionality. It defines 2 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `DataArray`: Performs DataArray operation as part of the component's functionality
- `HMemDataPool1`: Performs HMemDataPool1 operation as part of the component's functionality
### Kluczowe Funkcje
- `Free()`: Performs Free operation as part of the component's functionality
- `Detach()`: Performs Detach operation as part of the component's functionality
- `SetData()`: Aktualizuje Data w stanie wewnętrznym lub konfiguracji
- `Clear()`: Czyści wszystkie dane i resetuje do początkowego pustego stanu
- `LocateData()`: Evaluates conditions and returns boolean indicating success or validity
- `CreateData()`: Tworzy i inicjalizuje nowy Data instancję
- `DeleteData()`: Usuwa Data z kolekcji i zwalnia powiązane zasoby
- `GetData()`: Pobiera Data ze stanu wewnętrznego lub struktury danych
- `GetDataSize()`: Pobiera DataSize ze stanu wewnętrznego lub struktury danych
- `SaveItem()`: Zapisuje bieżący stan lub dane do trwałego magazynu
### Podsumowanie
Plik `hmdpool.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 2 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
