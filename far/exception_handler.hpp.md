# exception_handler.hpp
## English
### Purpose
This file implements functionality related to: */
Main functional areas: core functionality
### Key Classes
- `Plugin`: Performs Plugin operation as part of the component's functionality
- `invalid_parameter_handler`: Callback function that handles specific events or notifications
- `no_handler`: Callback function that handles specific events or notifications
- `save_exception_and_return`: Validates conditions and throws exceptions when errors are detected during save_exception_and_return operation
- `save_exception_to`: Validates conditions and throws exceptions when errors are detected during save_exception_to operation
- `seh_exception`: Performs seh_exception operation as part of the component's functionality
- `seh_exception_impl`: Performs seh_exception_impl operation as part of the component's functionality
- `signal_handler`: Callback function that handles specific events or notifications
- `unhandled_exception_filter`: Performs unhandled_exception_filter operation as part of the component's functionality
- `vectored_exception_handler`: Callback function that handles specific events or notifications
### Key Functions
- `report_to_stderr()`: Performs report_to_stderr operation as part of the component's functionality
- `set_report_location()`: Updates _report_location in internal state or configuration
- `disable_exception_handling()`: Performs disable_exception_handling operation as part of the component's functionality
- `exception_handling_in_progress()`: Performs exception_handling_in_progress operation as part of the component's functionality
- `force_stderr_exception_ui()`: Performs force_stderr_exception_ui operation as part of the component's functionality
- `restore_system_exception_handler()`: Callback function that handles specific events or notifications
- `set()`: Updates value in internal state or configuration
- `raise()`: Performs raise operation as part of the component's functionality
- `dismiss()`: Performs dismiss operation as part of the component's functionality
- `get()`: Retrieves value from internal state or data structure
### Namespaces
- `detail`
### Summary
The `exception_handler.hpp` file provides essential functionality for core functionality. It defines 10 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: */
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `Plugin`: Performs Plugin operation as part of the component's functionality
- `invalid_parameter_handler`: Callback function that handles specific events or notifications
- `no_handler`: Callback function that handles specific events or notifications
- `save_exception_and_return`: Validates conditions and throws exceptions when errors are detected during save_exception_and_return operation
- `save_exception_to`: Validates conditions and throws exceptions when errors are detected during save_exception_to operation
- `seh_exception`: Performs seh_exception operation as part of the component's functionality
- `seh_exception_impl`: Performs seh_exception_impl operation as part of the component's functionality
- `signal_handler`: Callback function that handles specific events or notifications
- `unhandled_exception_filter`: Performs unhandled_exception_filter operation as part of the component's functionality
- `vectored_exception_handler`: Callback function that handles specific events or notifications
### Kluczowe Funkcje
- `report_to_stderr()`: Performs report_to_stderr operation as part of the component's functionality
- `set_report_location()`: Aktualizuje _report_location w stanie wewnętrznym lub konfiguracji
- `disable_exception_handling()`: Performs disable_exception_handling operation as part of the component's functionality
- `exception_handling_in_progress()`: Performs exception_handling_in_progress operation as part of the component's functionality
- `force_stderr_exception_ui()`: Performs force_stderr_exception_ui operation as part of the component's functionality
- `restore_system_exception_handler()`: Callback function that handles specific events or notifications
- `set()`: Aktualizuje wartość w stanie wewnętrznym lub konfiguracji
- `raise()`: Performs raise operation as part of the component's functionality
- `dismiss()`: Performs dismiss operation as part of the component's functionality
- `get()`: Pobiera wartość ze stanu wewnętrznego lub struktury danych
### Przestrzenie nazw
- `detail`
### Podsumowanie
Plik `exception_handler.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 10 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
