# comutils.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `BStr`: Implements BStr functionality
- `ComBase`: Implements ComBase functionality
- `ComObject`: Implements ComObject functionality
- `PropVariant`: Implements PropVariant functionality
### Key Functions
- `Release()`: Implements Release functionality
- `bool()`: Implements bool functionality
- `detach()`: Implements detach functionality
- `clear()`: Implements clear functionality
- `ref()`: Implements ref functionality
- `set_binary()`: Updates _binary with provided value and validates constraints
- `is_int()`: Checks condition and returns boolean indicating state
- `is_uint()`: Processes input by dispatching to different code paths based on type or value
- `is_int64()`: Processes input by dispatching to different code paths based on type or value
- `is_uint64()`: Processes input by dispatching to different code paths based on type or value
### Summary
The `comutils.hpp` file provides essential functionality for core functionality. It defines 4 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `BStr`: Implements BStr functionality
- `ComBase`: Implements ComBase functionality
- `ComObject`: Implements ComObject functionality
- `PropVariant`: Implements PropVariant functionality
### Kluczowe Funkcje
- `Release()`: Implements Release functionality
- `bool()`: Implements bool functionality
- `detach()`: Implements detach functionality
- `clear()`: Implements clear functionality
- `ref()`: Implements ref functionality
- `set_binary()`: Updates _binary with provided value and validates constraints
- `is_int()`: Checks condition and returns boolean indicating state
- `is_uint()`: Processes input by dispatching to different code paths based on type or value
- `is_int64()`: Processes input by dispatching to different code paths based on type or value
- `is_uint64()`: Processes input by dispatching to different code paths based on type or value
### Podsumowanie
Plik `comutils.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 4 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
