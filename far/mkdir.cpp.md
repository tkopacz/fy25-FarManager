# mkdir.cpp
## English
### Purpose
This file implements functionality related to: Создание каталога
Main functional areas: core functionality
### Key Functions
- `MkDirDlgProc()`: Displays dialog interface for user interaction and input collection
- `create_directory()`: Creates and initializes new _directory instance
- `retryable_ui_operation()`: Performs retryable_ui_operation operation as part of the component's functionality
- `add_reparse_point()`: Adds new _reparse_point to collection or list
- `ShellMakeDirImpl()`: Displays dialog interface for user interaction and input collection
- `ShellMakeDir()`: Performs ShellMakeDir operation as part of the component's functionality
### Summary
The `mkdir.cpp` file provides essential functionality for core functionality. and contains approximately 6 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Создание каталога
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `MkDirDlgProc()`: Displays dialog interface for user interaction and input collection
- `create_directory()`: Tworzy i inicjalizuje nowy _directory instancję
- `retryable_ui_operation()`: Performs retryable_ui_operation operation as part of the component's functionality
- `add_reparse_point()`: Dodaje nowy _reparse_point do kolekcji lub listy
- `ShellMakeDirImpl()`: Displays dialog interface for user interaction and input collection
- `ShellMakeDir()`: Performs ShellMakeDir operation as part of the component's functionality
### Podsumowanie
Plik `mkdir.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 6 funkcji wspierających operacje menedżera plików Far Manager.
