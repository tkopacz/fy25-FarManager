# plugin.cpp
## English
### Purpose
Main functional areas: plugin interface
### Key Classes
- `Plugin`: Performs Plugin operation in plugin.cpp
- `PluginPanelItemAccessor`: Performs PluginPanelItemAccessor operation in plugin.cpp
- `PluginPanelItems`: Performs PluginPanelItems operation in plugin.cpp
### Key Functions
- `open()`: Performs open operation in plugin.cpp
- `guess_fs_ext()`: Performs guess_fs_ext operation in plugin.cpp
- `correct_part_root_name()`: 0xEF53 -- ExtFS superblock_magic
- `part_index_to_item()`: N.(mpr|gpt).EXT -- already converted
- `set_partition()`: Checks a condition in plugin.cpp and returns true or false based on the result
- `info()`: Checks a condition in plugin.cpp and returns true or false based on the result
- `set_dir()`: Iterates through elements in plugin.cpp and adds them to a collection
- `list()`: Performs list operation in plugin.cpp
- `get_separate_dir_path()`: Retrieves _separate_dir_path value in plugin.cpp
- `extract()`: Iterates through elements in plugin.cpp and adds them to a collection
### Summary
The `plugin.cpp` file provides essential functionality for plugin interface. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: interfejs wtyczek
### Kluczowe Klasy
- `Plugin`: Performs Plugin operation in plugin.cpp
- `PluginPanelItemAccessor`: Performs PluginPanelItemAccessor operation in plugin.cpp
- `PluginPanelItems`: Performs PluginPanelItems operation in plugin.cpp
### Kluczowe Funkcje
- `open()`: Performs open operation in plugin.cpp
- `guess_fs_ext()`: Performs guess_fs_ext operation in plugin.cpp
- `correct_part_root_name()`: 0xEF53 -- ExtFS superblock_magic
- `part_index_to_item()`: N.(mpr|gpt).EXT -- already converted
- `set_partition()`: Checks a condition in plugin.cpp and returns true or false based on the result
- `info()`: Checks a condition in plugin.cpp and returns true or false based on the result
- `set_dir()`: Iterates through elements in plugin.cpp and adds them to a collection
- `list()`: Performs list operation in plugin.cpp
- `get_separate_dir_path()`: Retrieves _separate_dir_path value in plugin.cpp
- `extract()`: Iterates through elements in plugin.cpp and adds them to a collection
### Podsumowanie
Plik `plugin.cpp` zapewnia podstawową funkcjonalność dla interfejs wtyczek. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
