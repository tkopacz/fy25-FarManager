# macroapi.cpp
## English
### Purpose
This file implements functionality related to: Macro API
Main functional areas: core functionality
### Key Classes
- `FarMacroApi`: Performs FarMacroApi operation as part of the component's functionality
- `LockOutput`: Performs LockOutput operation as part of the component's functionality
- `editor_options`: Performs editor_options operation as part of the component's functionality
### Key Functions
- `ToDouble()`: Evaluates conditions and returns a boolean indicating success or validity of ToDouble
- `SelectPanel()`: Performs SelectPanel operation as part of the component's functionality
- `PushBoolean()`: Evaluates conditions and returns boolean indicating success or validity
- `PushPointer()`: Performs PushPointer operation as part of the component's functionality
- `PushValue()`: Performs PushValue operation as part of the component's functionality
- `PushNil()`: Performs PushNil operation as part of the component's functionality
- `PushTable()`: Performs PushTable operation as part of the component's functionality
- `SetTable()`: Updates Table in internal state or configuration
- `PushError()`: Performs PushError operation as part of the component's functionality
- `SetField()`: Updates Field in internal state or configuration
### Summary
The `macroapi.cpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Macro API
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `FarMacroApi`: Performs FarMacroApi operation as part of the component's functionality
- `LockOutput`: Performs LockOutput operation as part of the component's functionality
- `editor_options`: Performs editor_options operation as part of the component's functionality
### Kluczowe Funkcje
- `ToDouble()`: Evaluates conditions and returns a boolean indicating success or validity of ToDouble
- `SelectPanel()`: Performs SelectPanel operation as part of the component's functionality
- `PushBoolean()`: Evaluates conditions and returns boolean indicating success or validity
- `PushPointer()`: Performs PushPointer operation as part of the component's functionality
- `PushValue()`: Performs PushValue operation as part of the component's functionality
- `PushNil()`: Performs PushNil operation as part of the component's functionality
- `PushTable()`: Performs PushTable operation as part of the component's functionality
- `SetTable()`: Aktualizuje Table w stanie wewnętrznym lub konfiguracji
- `PushError()`: Performs PushError operation as part of the component's functionality
- `SetField()`: Aktualizuje Field w stanie wewnętrznym lub konfiguracji
### Podsumowanie
Plik `macroapi.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
