# mu_case.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `ToUpper()`: Performs ToUpper operation as part of the component's functionality
- `ToLower()`: Performs ToLower operation as part of the component's functionality
- `isLower()`: Tests whether Lower condition is true or property exists
- `isUpper()`: Tests whether Upper condition is true or property exists
- `StrLwr()`: Performs StrLwr operation as part of the component's functionality
- `StrUpr()`: Performs StrUpr operation as part of the component's functionality
- `isSpace()`: Tests whether Space condition is true or property exists
- `StrCase()`: Performs StrCase operation as part of the component's functionality
- `memicmp()`: Performs memicmp operation as part of the component's functionality
- `BuffCmp()`: Evaluates conditions and returns boolean indicating success or validity
### Summary
The `mu_case.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `ToUpper()`: Performs ToUpper operation as part of the component's functionality
- `ToLower()`: Performs ToLower operation as part of the component's functionality
- `isLower()`: Testuje czy Lower warunek jest prawdziwy lub właściwość istnieje
- `isUpper()`: Testuje czy Upper warunek jest prawdziwy lub właściwość istnieje
- `StrLwr()`: Performs StrLwr operation as part of the component's functionality
- `StrUpr()`: Performs StrUpr operation as part of the component's functionality
- `isSpace()`: Testuje czy Space warunek jest prawdziwy lub właściwość istnieje
- `StrCase()`: Performs StrCase operation as part of the component's functionality
- `memicmp()`: Performs memicmp operation as part of the component's functionality
- `BuffCmp()`: Evaluates conditions and returns boolean indicating success or validity
### Podsumowanie
Plik `mu_case.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
