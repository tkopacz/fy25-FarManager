# RegExp.hpp
## English
### Purpose
This file implements functionality related to: Regular expressions
Main functional areas: regular expressions
### Key Classes
- `REOpCode`: Performs REOpCode operation in RegExp.hpp
- `RegExp`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `StateStackItem`: Performs StateStackItem operation in RegExp.hpp
- `UniSet`: Performs UniSet operation in RegExp.hpp
- `regex_exception`: ! Perl style RegExp provided. i.e. /expression/imsx
- `regex_match`: Performs regex_match operation in RegExp.hpp
- `state_stack`: Performs state_stack operation in RegExp.hpp
### Key Functions
- `code()`: Performs code operation in RegExp.hpp
- `position()`: Performs position operation in RegExp.hpp
- `to_string()`: Performs to_string operation in RegExp.hpp
- `CalcLength()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `InnerCompile()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `InnerMatch()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `TrimTail()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `StrCmp()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `Compile()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `Optimize()`: ! Try to optimize regular expression
### Summary
The `RegExp.hpp` file provides essential functionality for regular expressions. It defines 7 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Regular expressions
Główne obszary funkcjonalne: wyrażenia regularne
### Kluczowe Klasy
- `REOpCode`: Performs REOpCode operation in RegExp.hpp
- `RegExp`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `StateStackItem`: Performs StateStackItem operation in RegExp.hpp
- `UniSet`: Performs UniSet operation in RegExp.hpp
- `regex_exception`: ! Perl style RegExp provided. i.e. /expression/imsx
- `regex_match`: Performs regex_match operation in RegExp.hpp
- `state_stack`: Performs state_stack operation in RegExp.hpp
### Kluczowe Funkcje
- `code()`: Performs code operation in RegExp.hpp
- `position()`: Performs position operation in RegExp.hpp
- `to_string()`: Performs to_string operation in RegExp.hpp
- `CalcLength()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `InnerCompile()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `InnerMatch()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `TrimTail()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `StrCmp()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `Compile()`: Validates a condition in RegExp.hpp and throws an exception if the validation fails
- `Optimize()`: ! Try to optimize regular expression
### Podsumowanie
Plik `RegExp.hpp` zapewnia podstawową funkcjonalność dla wyrażenia regularne. Definiuje 7 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
