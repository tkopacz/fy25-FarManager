# EMenu.cpp
## English
### Purpose
Main functional areas: menu management
### Key Functions
- `GetGlobalInfoW()`: Retrieves GlobalInfoW from internal state or data structure
- `SetStartupInfoW()`: Updates StartupInfoW in internal state or configuration
- `CPlugin()`: Performs CPlugin operation as part of the component's functionality
- `GetPluginInfoW()`: Retrieves PluginInfoW from internal state or data structure
- `OpenW()`: Opens resource and prepares it for access operations
- `ConfigureW()`: Performs ConfigureW operation as part of the component's functionality
- `ExitFARW()`: Performs ExitFARW operation as part of the component's functionality
### Summary
The `EMenu.cpp` file provides essential functionality for menu management. and contains approximately 7 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: zarządzanie menu
### Kluczowe Funkcje
- `GetGlobalInfoW()`: Pobiera GlobalInfoW ze stanu wewnętrznego lub struktury danych
- `SetStartupInfoW()`: Aktualizuje StartupInfoW w stanie wewnętrznym lub konfiguracji
- `CPlugin()`: Wykonuje CPlugin operację jako część zarządzanie menu
- `GetPluginInfoW()`: Pobiera PluginInfoW ze stanu wewnętrznego lub struktury danych
- `OpenW()`: Otwiera zasób i przygotowuje go do operacji dostępu
- `ConfigureW()`: Wykonuje ConfigureW operację jako część zarządzanie menu
- `ExitFARW()`: Wykonuje ExitFARW operację jako część zarządzanie menu
### Podsumowanie
Plik `EMenu.cpp` zapewnia podstawową funkcjonalność dla zarządzanie menu. i zawiera około 7 funkcji wspierających operacje menedżera plików Far Manager.
