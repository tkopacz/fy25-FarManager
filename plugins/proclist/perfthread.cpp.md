# perfthread.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `PROCLIST_SYSTEM_PROCESS_INFORMATION`: Performs PROCLIST_SYSTEM_PROCESS_INFORMATION operation as part of the component's functionality
- `RegKey`: Accesses Windows registry to read or write configuration data
### Key Functions
- `HKEY()`: Performs HKEY operation as part of the component's functionality
- `getcounter()`: Retrieves counter from internal state or data structure
- `Is64BitWindows()`: Tests whether 64BitWindows condition is true or property exists
- `is_wow64_itself()`: Tests whether _wow64_itself condition is true or property exists
- `l()`: Evaluates conditions and returns a boolean indicating success or validity of l
- `get_logical_processor_count()`: Retrieves _logical_processor_count from internal state or data structure
- `HRESULT_FROM_WIN32()`: Performs HRESULT_FROM_WIN32 operation as part of the component's functionality
### Summary
The `perfthread.cpp` file provides essential functionality for core functionality. It defines 2 class(es) and contains approximately 7 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `PROCLIST_SYSTEM_PROCESS_INFORMATION`: Performs PROCLIST_SYSTEM_PROCESS_INFORMATION operation as part of the component's functionality
- `RegKey`: Accesses Windows registry to read or write configuration data
### Kluczowe Funkcje
- `HKEY()`: Performs HKEY operation as part of the component's functionality
- `getcounter()`: Pobiera counter ze stanu wewnętrznego lub struktury danych
- `Is64BitWindows()`: Testuje czy 64BitWindows warunek jest prawdziwy lub właściwość istnieje
- `is_wow64_itself()`: Testuje czy _wow64_itself warunek jest prawdziwy lub właściwość istnieje
- `l()`: Evaluates conditions and returns a boolean indicating success or validity of l
- `get_logical_processor_count()`: Pobiera _logical_processor_count ze stanu wewnętrznego lub struktury danych
- `HRESULT_FROM_WIN32()`: Performs HRESULT_FROM_WIN32 operation as part of the component's functionality
### Podsumowanie
Plik `perfthread.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 2 klas(y) i zawiera około 7 funkcji wspierających operacje menedżera plików Far Manager.
