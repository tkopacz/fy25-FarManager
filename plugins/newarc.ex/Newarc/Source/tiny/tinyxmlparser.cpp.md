# tinyxmlparser.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `TiXmlDocument`: Performs TiXmlDocument operation as part of the component's functionality
- `TiXmlParsingData`: Performs TiXmlParsingData operation as part of the component's functionality
### Key Functions
- `isalpha()`: Tests whether alpha condition is true or property exists
- `isalnum()`: Tests whether alnum condition is true or property exists
- `Stamp()`: Performs Stamp operation as part of the component's functionality
- `Cursor()`: Constructor that initializes Cursor object with provided parameters
- `ConvertUTF32ToUTF8()`: Converts data from one format or type to another
- `TIXML_LOG()`: Performs TIXML_LOG operation as part of the component's functionality
- `TiXmlDeclaration()`: Performs TiXmlDeclaration operation as part of the component's functionality
- `TiXmlComment()`: Performs TiXmlComment operation as part of the component's functionality
- `TiXmlText()`: Performs TiXmlText operation as part of the component's functionality
- `TiXmlUnknown()`: Performs TiXmlUnknown operation as part of the component's functionality
### Summary
The `tinyxmlparser.cpp` file provides essential functionality for core functionality. It defines 2 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `TiXmlDocument`: Performs TiXmlDocument operation as part of the component's functionality
- `TiXmlParsingData`: Performs TiXmlParsingData operation as part of the component's functionality
### Kluczowe Funkcje
- `isalpha()`: Testuje czy alpha warunek jest prawdziwy lub właściwość istnieje
- `isalnum()`: Testuje czy alnum warunek jest prawdziwy lub właściwość istnieje
- `Stamp()`: Performs Stamp operation as part of the component's functionality
- `Cursor()`: Konstruktor inicjalizujący Cursor obiekt z dostarczonymi parametrami
- `ConvertUTF32ToUTF8()`: Konwertuje dane z jednego formatu lub typu do innego
- `TIXML_LOG()`: Performs TIXML_LOG operation as part of the component's functionality
- `TiXmlDeclaration()`: Performs TiXmlDeclaration operation as part of the component's functionality
- `TiXmlComment()`: Performs TiXmlComment operation as part of the component's functionality
- `TiXmlText()`: Performs TiXmlText operation as part of the component's functionality
- `TiXmlUnknown()`: Performs TiXmlUnknown operation as part of the component's functionality
### Podsumowanie
Plik `tinyxmlparser.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 2 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
