# io_attr.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `isReadOnly()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `ClrReadOnly()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `SetReadOnly()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `SetCurRights()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `GetFileAttr()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `SetFileAttr()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `FIO_CHMOD()`: Performs FIO_CHMOD operation in io_attr.cpp
- `WinFName()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `Stub_rtl_open()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `Stub_rtl_creat()`: Checks a condition in io_attr.cpp and returns true or false based on the result
### Summary
The `io_attr.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `isReadOnly()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `ClrReadOnly()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `SetReadOnly()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `SetCurRights()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `GetFileAttr()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `SetFileAttr()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `FIO_CHMOD()`: Performs FIO_CHMOD operation in io_attr.cpp
- `WinFName()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `Stub_rtl_open()`: Checks a condition in io_attr.cpp and returns true or false based on the result
- `Stub_rtl_creat()`: Checks a condition in io_attr.cpp and returns true or false based on the result
### Podsumowanie
Plik `io_attr.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
