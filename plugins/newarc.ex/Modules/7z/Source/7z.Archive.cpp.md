# 7z.Archive.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `PropertyToName`: Performs PropertyToName operation as part of the component's functionality
### Key Functions
- `CInFile()`: Performs CInFile operation as part of the component's functionality
- `Callback()`: Evaluates conditions and returns a boolean indicating success or validity of Callback
- `VariantToInt64()`: Performs VariantToInt64 operation as part of the component's functionality
- `CreateTempName()`: Creates and initializes new TempName instance
- `COutFile()`: Performs COutFile operation as part of the component's functionality
- `DeleteFile()`: Removes File from collection and frees associated resources
- `CompareIndicies()`: Performs CompareIndicies operation as part of the component's functionality
- `m_pfnCallback()`: Callback function that handles specific events or notifications
### Summary
The `7z.Archive.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 8 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `PropertyToName`: Performs PropertyToName operation as part of the component's functionality
### Kluczowe Funkcje
- `CInFile()`: Performs CInFile operation as part of the component's functionality
- `Callback()`: Evaluates conditions and returns a boolean indicating success or validity of Callback
- `VariantToInt64()`: Performs VariantToInt64 operation as part of the component's functionality
- `CreateTempName()`: Tworzy i inicjalizuje nowy TempName instancję
- `COutFile()`: Performs COutFile operation as part of the component's functionality
- `DeleteFile()`: Usuwa File z kolekcji i zwalnia powiązane zasoby
- `CompareIndicies()`: Performs CompareIndicies operation as part of the component's functionality
- `m_pfnCallback()`: Callback function that handles specific events or notifications
### Podsumowanie
Plik `7z.Archive.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 8 funkcji wspierających operacje menedżera plików Far Manager.
