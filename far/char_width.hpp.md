# char_width.hpp
## English
### Purpose
This file implements functionality related to: Fullwidth support
Main functional areas: core functionality
### Key Functions
- `get()`: Retrieves  value in char_width.hpp
- `is_wide()`: Checks a condition in char_width.hpp and returns the result
- `enable()`: Performs enable operation in char_width.hpp
- `is_enabled()`: Checks a condition in char_width.hpp and returns the result
- `invalidate()`: Performs invalidate operation in char_width.hpp
- `is_half_width_surrogate_broken()`: Checks a condition in char_width.hpp and returns the result
- `is_grapheme_clusters_on()`: Checks a condition in char_width.hpp and returns the result
### Namespaces
- `char_width`
### Summary
The `char_width.hpp` file provides essential functionality for core functionality. and contains approximately 7 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Fullwidth support
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `get()`: Retrieves  value in char_width.hpp
- `is_wide()`: Checks a condition in char_width.hpp and returns the result
- `enable()`: Performs enable operation in char_width.hpp
- `is_enabled()`: Checks a condition in char_width.hpp and returns the result
- `invalidate()`: Performs invalidate operation in char_width.hpp
- `is_half_width_surrogate_broken()`: Checks a condition in char_width.hpp and returns the result
- `is_grapheme_clusters_on()`: Checks a condition in char_width.hpp and returns the result
### Przestrzenie nazw
- `char_width`
### Podsumowanie
Plik `char_width.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 7 funkcji wspierających operacje menedżera plików Far Manager.
