# filecvt.cpp
## English
### Purpose
Main functional areas: file system operations
### Key Functions
- `DlgProc()`: Processes input by dispatching to different code paths based on type or value
- `CaseConvertion()`: Processes input by dispatching to different code paths based on type or value
- `Builder()`: Processes input by dispatching to different code paths based on type or value
- `settings()`: Processes input by dispatching to different code paths based on type or value
### Summary
The `filecvt.cpp` file provides essential functionality for file system operations. and contains approximately 4 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Funkcje
- `DlgProc()`: Processes input by dispatching to different code paths based on type or value
- `CaseConvertion()`: Processes input by dispatching to different code paths based on type or value
- `Builder()`: Processes input by dispatching to different code paths based on type or value
- `settings()`: Processes input by dispatching to different code paths based on type or value
### Podsumowanie
Plik `filecvt.cpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. i zawiera około 4 funkcji wspierających operacje menedżera plików Far Manager.
