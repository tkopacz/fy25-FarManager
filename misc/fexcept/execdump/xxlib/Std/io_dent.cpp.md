# io_dent.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `isDirectory()`: Checks a condition in io_dent.cpp and returns true or false based on the result
- `CreateFullDirectory()`: Checks a condition in io_dent.cpp and returns true or false based on the result
- `SET_FLAG()`: Checks a condition in io_dent.cpp and returns true or false based on the result
### Summary
The `io_dent.cpp` file provides essential functionality for core functionality. and contains approximately 3 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `isDirectory()`: Checks a condition in io_dent.cpp and returns true or false based on the result
- `CreateFullDirectory()`: Checks a condition in io_dent.cpp and returns true or false based on the result
- `SET_FLAG()`: Checks a condition in io_dent.cpp and returns true or false based on the result
### Podsumowanie
Plik `io_dent.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 3 funkcji wspierających operacje menedżera plików Far Manager.
