# Mix.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `AddWait()`: Adds new Wait to collection or list
- `IsAbsolutePath()`: Tests whether AbsolutePath condition is true or property exists
- `QuoteStr()`: Performs QuoteStr operation as part of the component's functionality
- `Size2Str()`: Returns the current size or capacity of the container
- `Str2Size()`: Returns the current size or capacity of the container
- `StrSlashCount()`: Performs StrSlashCount operation as part of the component's functionality
- `FTestOpen()`: Opens and manages file access for reading or writing operations
- `FRealFile()`: Opens and manages file access for reading or writing operations
- `DoCreateDirectory()`: Evaluates conditions and returns a boolean indicating success or validity of DoCreateDirectory
- `Fsize()`: Opens and manages file access for reading or writing operations
### Summary
The `Mix.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `AddWait()`: Dodaje nowy Wait do kolekcji lub listy
- `IsAbsolutePath()`: Testuje czy AbsolutePath warunek jest prawdziwy lub właściwość istnieje
- `QuoteStr()`: Performs QuoteStr operation as part of the component's functionality
- `Size2Str()`: Returns the current size or capacity of the container
- `Str2Size()`: Returns the current size or capacity of the container
- `StrSlashCount()`: Performs StrSlashCount operation as part of the component's functionality
- `FTestOpen()`: Opens and manages file access for reading or writing operations
- `FRealFile()`: Opens and manages file access for reading or writing operations
- `DoCreateDirectory()`: Evaluates conditions and returns a boolean indicating success or validity of DoCreateDirectory
- `Fsize()`: Opens and manages file access for reading or writing operations
### Podsumowanie
Plik `Mix.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
