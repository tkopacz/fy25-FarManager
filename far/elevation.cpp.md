# elevation.cpp
## English
### Purpose
This file implements functionality related to: Elevation
Main functional areas: core functionality
### Key Classes
- `EAData`: Performs EAData operation in elevation.cpp
- `callback_param`: Performs callback_param operation in elevation.cpp
- `elevated`: Performs elevated operation in elevation.cpp
- `security_attributes_wrapper`: Performs security_attributes_wrapper operation in elevation.cpp
### Key Functions
- `CreateBackupRestorePrivilege()`: Opens a file or creates a file handle in elevation.cpp
- `WritePipe()`: Opens a file or creates a file handle in elevation.cpp
- `ReadPipe()`: Opens a file or creates a file handle in elevation.cpp
- `set_attributes()`: Sets or updates _attributes value in elevation.cpp
- `set_descriptor()`: Sets or updates _descriptor value in elevation.cpp
- `PrivilegedHander()`: Performs PrivilegedHander operation in elevation.cpp
- `ElevatedHandler()`: Performs ElevatedHandler operation in elevation.cpp
- `TerminateChildProcess()`: Checks a condition in elevation.cpp and returns true or false based on the result
- `make_admin_sid()`: Something went really bad, it's better to stop any further attempts
- `make_explicit_admin_access()`: Something went really bad, it's better to stop any further attempts
### Summary
The `elevation.cpp` file provides essential functionality for core functionality. It defines 4 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Elevation
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `EAData`: Performs EAData operation in elevation.cpp
- `callback_param`: Performs callback_param operation in elevation.cpp
- `elevated`: Performs elevated operation in elevation.cpp
- `security_attributes_wrapper`: Performs security_attributes_wrapper operation in elevation.cpp
### Kluczowe Funkcje
- `CreateBackupRestorePrivilege()`: Opens a file or creates a file handle in elevation.cpp
- `WritePipe()`: Opens a file or creates a file handle in elevation.cpp
- `ReadPipe()`: Opens a file or creates a file handle in elevation.cpp
- `set_attributes()`: Sets or updates _attributes value in elevation.cpp
- `set_descriptor()`: Sets or updates _descriptor value in elevation.cpp
- `PrivilegedHander()`: Performs PrivilegedHander operation in elevation.cpp
- `ElevatedHandler()`: Performs ElevatedHandler operation in elevation.cpp
- `TerminateChildProcess()`: Checks a condition in elevation.cpp and returns true or false based on the result
- `make_admin_sid()`: Something went really bad, it's better to stop any further attempts
- `make_explicit_admin_access()`: Something went really bad, it's better to stop any further attempts
### Podsumowanie
Plik `elevation.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 4 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
