# FormatDetector.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `Is7zHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsArjHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsCabHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsHfsHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsIsoHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsLzhHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsLzmaHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsMachoHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsNsisHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsPeHeader()`: Checks a condition in FormatDetector.cpp and returns the result
### Summary
The `FormatDetector.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `Is7zHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsArjHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsCabHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsHfsHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsIsoHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsLzhHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsLzmaHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsMachoHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsNsisHeader()`: Checks a condition in FormatDetector.cpp and returns the result
- `IsPeHeader()`: Checks a condition in FormatDetector.cpp and returns the result
### Podsumowanie
Plik `FormatDetector.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
