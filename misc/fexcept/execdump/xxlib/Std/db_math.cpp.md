# db_math.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `FMod()`: FMod( x,y ) - Calculate reminder of x/y
- `BiggestRound()`: Performs BiggestRound operation in db_math.cpp
- `main()`: Main entry point in db_math.cpp that starts program execution
- `HDB_RectPart()`: Performs HDB_RectPart operation in db_math.cpp
- `HDB_StrCorrect()`: Performs HDB_StrCorrect operation in db_math.cpp
- `HDB_Str2Db()`: Checks a condition in db_math.cpp and returns true or false based on the result
- `HDB_Str2Point()`: Checks a condition in db_math.cpp and returns true or false based on the result
- `HDB_Str2Rect()`: Checks a condition in db_math.cpp and returns true or false based on the result
- `HDB_Rect2Str()`: Checks a condition in db_math.cpp and returns true or false based on the result
- `MyString()`: Performs MyString operation in db_math.cpp
### Summary
The `db_math.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `FMod()`: FMod( x,y ) - Calculate reminder of x/y
- `BiggestRound()`: Performs BiggestRound operation in db_math.cpp
- `main()`: Main entry point in db_math.cpp that starts program execution
- `HDB_RectPart()`: Performs HDB_RectPart operation in db_math.cpp
- `HDB_StrCorrect()`: Performs HDB_StrCorrect operation in db_math.cpp
- `HDB_Str2Db()`: Checks a condition in db_math.cpp and returns true or false based on the result
- `HDB_Str2Point()`: Checks a condition in db_math.cpp and returns true or false based on the result
- `HDB_Str2Rect()`: Checks a condition in db_math.cpp and returns true or false based on the result
- `HDB_Rect2Str()`: Checks a condition in db_math.cpp and returns true or false based on the result
- `MyString()`: Performs MyString operation in db_math.cpp
### Podsumowanie
Plik `db_math.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
