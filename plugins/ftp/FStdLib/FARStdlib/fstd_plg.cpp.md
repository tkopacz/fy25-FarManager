# fstd_plg.cpp
## English
### Purpose
Main functional areas: file system operations
### Key Functions
- `idAtExit()`: Performs idAtExit operation in fstd_plg.cpp
- `FP_SetStartupInfo()`: Performs FP_SetStartupInfo operation in fstd_plg.cpp
- `DllMain()`: DLL entry point in fstd_plg.cpp that handles process/thread attach and detach notifications
### Summary
The `fstd_plg.cpp` file provides essential functionality for file system operations. and contains approximately 3 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Funkcje
- `idAtExit()`: Performs idAtExit operation in fstd_plg.cpp
- `FP_SetStartupInfo()`: Performs FP_SetStartupInfo operation in fstd_plg.cpp
- `DllMain()`: DLL entry point in fstd_plg.cpp that handles process/thread attach and detach notifications
### Podsumowanie
Plik `fstd_plg.cpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. i zawiera około 3 funkcji wspierających operacje menedżera plików Far Manager.
