# ui.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `OverwriteFileInfo`: Performs OverwriteFileInfo operation as part of the component's functionality
- `OverwriteOptions`: Performs OverwriteOptions operation as part of the component's functionality
- `PluginSettings`: Performs PluginSettings operation as part of the component's functionality
### Key Functions
- `password_dialog()`: Performs password_dialog operation as part of the component's functionality
- `overwrite_dialog()`: Performs overwrite_dialog operation as part of the component's functionality
- `extract_dialog()`: Performs extract_dialog operation as part of the component's functionality
- `show_error_log()`: Displays content or makes visual element visible on screen
- `update_dialog()`: Updates existing data with new values while preserving structure
- `settings_dialog()`: Updates tings_dialog in internal state or configuration
- `attr_dialog()`: Performs attr_dialog operation as part of the component's functionality
- `sfx_options_dialog()`: Performs sfx_options_dialog operation as part of the component's functionality
### Summary
The `ui.hpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 8 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `OverwriteFileInfo`: Performs OverwriteFileInfo operation as part of the component's functionality
- `OverwriteOptions`: Performs OverwriteOptions operation as part of the component's functionality
- `PluginSettings`: Performs PluginSettings operation as part of the component's functionality
### Kluczowe Funkcje
- `password_dialog()`: Performs password_dialog operation as part of the component's functionality
- `overwrite_dialog()`: Performs overwrite_dialog operation as part of the component's functionality
- `extract_dialog()`: Performs extract_dialog operation as part of the component's functionality
- `show_error_log()`: Wyświetla zawartość lub czyni element wizualny widocznym na ekranie
- `update_dialog()`: Aktualizuje istniejące dane nowymi wartościami zachowując strukturę
- `settings_dialog()`: Aktualizuje tings_dialog w stanie wewnętrznym lub konfiguracji
- `attr_dialog()`: Performs attr_dialog operation as part of the component's functionality
- `sfx_options_dialog()`: Performs sfx_options_dialog operation as part of the component's functionality
### Podsumowanie
Plik `ui.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 8 funkcji wspierających operacje menedżera plików Far Manager.
