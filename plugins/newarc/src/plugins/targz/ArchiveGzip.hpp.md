# ArchiveGzip.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `TarBzip`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `TarGzip`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `TarPlain`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
### Key Functions
- `read()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `ReadFile()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `error()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `seek()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `SetFilePointer()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `pgzseek()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
### Summary
The `ArchiveGzip.hpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 6 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `TarBzip`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `TarGzip`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `TarPlain`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
### Kluczowe Funkcje
- `read()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `ReadFile()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `error()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `seek()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `SetFilePointer()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
- `pgzseek()`: Opens and manages a file in ArchiveGzip.hpp, handling resource cleanup
### Podsumowanie
Plik `ArchiveGzip.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 6 funkcji wspierających operacje menedżera plików Far Manager.
