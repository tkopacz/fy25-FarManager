# codepage.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `installed_codepages`: Performs installed_codepages operation as part of the component's functionality
### Key Functions
- `GetACP()`: Retrieves ACP from internal state or data structure
- `GetOEMCP()`: Retrieves OEMCP from internal state or data structure
- `oem()`: Performs oem operation as part of the component's functionality
- `ansi()`: Performs ansi operation as part of the component's functionality
- `extract_codepage_name()`: Performs extract_codepage_name operation as part of the component's functionality
- `callback()`: Evaluates conditions and returns boolean indicating success or validity
- `enum_cp_callback()`: Validates conditions and throws exceptions when errors are detected during enum_cp_callback operation
- `cpp_try()`: Performs cpp_try operation as part of the component's functionality
- `get_installed_codepages()`: Retrieves _installed_codepages from internal state or data structure
- `InstalledCodepages()`: Performs InstalledCodepages operation as part of the component's functionality
### Summary
The `codepage.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `installed_codepages`: Performs installed_codepages operation as part of the component's functionality
### Kluczowe Funkcje
- `GetACP()`: Pobiera ACP ze stanu wewnętrznego lub struktury danych
- `GetOEMCP()`: Pobiera OEMCP ze stanu wewnętrznego lub struktury danych
- `oem()`: Performs oem operation as part of the component's functionality
- `ansi()`: Performs ansi operation as part of the component's functionality
- `extract_codepage_name()`: Performs extract_codepage_name operation as part of the component's functionality
- `callback()`: Evaluates conditions and returns boolean indicating success or validity
- `enum_cp_callback()`: Validates conditions and throws exceptions when errors are detected during enum_cp_callback operation
- `cpp_try()`: Performs cpp_try operation as part of the component's functionality
- `get_installed_codepages()`: Pobiera _installed_codepages ze stanu wewnętrznego lub struktury danych
- `InstalledCodepages()`: Performs InstalledCodepages operation as part of the component's functionality
### Podsumowanie
Plik `codepage.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
