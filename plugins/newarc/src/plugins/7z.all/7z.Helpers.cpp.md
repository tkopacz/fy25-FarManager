# 7z.Helpers.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `CCryptoGetTextPassword()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `CreateDirs()`: if( !FileExists (FullPath) )
- `GetItemIndex()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `strcat()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `COutFile()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `CArchiveOpenVolumeCallback()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `CheckForEsc()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `CInFile()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `wcscpy()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
### Summary
The `7z.Helpers.cpp` file provides essential functionality for core functionality. and contains approximately 9 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `CCryptoGetTextPassword()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `CreateDirs()`: if( !FileExists (FullPath) )
- `GetItemIndex()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `strcat()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `COutFile()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `CArchiveOpenVolumeCallback()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `CheckForEsc()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `CInFile()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
- `wcscpy()`: Opens and manages a file in 7z.Helpers.cpp, handling resource cleanup
### Podsumowanie
Plik `7z.Helpers.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 9 funkcji wspierających operacje menedżera plików Far Manager.
