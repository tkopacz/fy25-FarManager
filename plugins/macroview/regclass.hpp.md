# regclass.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `TReg`: Performs TReg operation as part of the component's functionality
- `TRegDataInfo`: Performs TRegDataInfo operation as part of the component's functionality
- `TRegKeyInfo`: Performs TRegKeyInfo operation as part of the component's functionality
### Key Functions
- `ChangeKey()`: Performs ChangeKey operation as part of the component's functionality
- `GetBaseKey()`: Retrieves BaseKey from internal state or data structure
- `GetKey()`: Retrieves Key from internal state or data structure
- `SetCurrentKey()`: Updates CurrentKey in internal state or configuration
- `MoveValue()`: Moves data or object from current location to new location
- `CopyValues()`: Copies data from source to destination location
- `CopyKeys()`: Copies data from source to destination location
- `CloseKey()`: Closes resource and performs cleanup operations
- `SetRootKey()`: Updates RootKey in internal state or configuration
- `CreateKey()`: Creates and initializes new Key instance
### Summary
The `regclass.hpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `TReg`: Performs TReg operation as part of the component's functionality
- `TRegDataInfo`: Performs TRegDataInfo operation as part of the component's functionality
- `TRegKeyInfo`: Performs TRegKeyInfo operation as part of the component's functionality
### Kluczowe Funkcje
- `ChangeKey()`: Performs ChangeKey operation as part of the component's functionality
- `GetBaseKey()`: Pobiera BaseKey ze stanu wewnętrznego lub struktury danych
- `GetKey()`: Pobiera Key ze stanu wewnętrznego lub struktury danych
- `SetCurrentKey()`: Aktualizuje CurrentKey w stanie wewnętrznym lub konfiguracji
- `MoveValue()`: Przenosi dane lub obiekt z bieżącej lokalizacji do nowej lokalizacji
- `CopyValues()`: Kopiuje dane ze źródła do miejsca docelowego
- `CopyKeys()`: Kopiuje dane ze źródła do miejsca docelowego
- `CloseKey()`: Zamyka zasób i wykonuje operacje czyszczące
- `SetRootKey()`: Aktualizuje RootKey w stanie wewnętrznym lub konfiguracji
- `CreateKey()`: Tworzy i inicjalizuje nowy Key instancję
### Podsumowanie
Plik `regclass.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
