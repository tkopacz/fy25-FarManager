# TmpClass.cpp
## English
### Purpose
This file implements functionality related to: Temporary panel plugin class implementation
Main functional areas: core functionality
### Key Functions
- `IsLinksDisplayed()`: Temporary panel plugin class implementation
- `IsOwnersDisplayed()`: Checks a condition in TmpClass.cpp and returns the result
- `NullToEmpty()`: Performs NullToEmpty operation in TmpClass.cpp
- `ColumnTypes()`: Performs ColumnTypes operation in TmpClass.cpp
- `PutDirectoryContents()`: Checks a condition in TmpClass.cpp and returns true or false based on the result
- `FullName()`: Performs FullName operation in TmpClass.cpp
- `GoToFile()`: Performs GoToFile operation in TmpClass.cpp
- `Buffer()`: Iterates through elements in TmpClass.cpp and adds them to a collection
- `SaveListFile()`: REQUIRED, we don't know the size
### Summary
The `TmpClass.cpp` file provides essential functionality for core functionality. and contains approximately 9 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Temporary panel plugin class implementation
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `IsLinksDisplayed()`: Temporary panel plugin class implementation
- `IsOwnersDisplayed()`: Checks a condition in TmpClass.cpp and returns the result
- `NullToEmpty()`: Performs NullToEmpty operation in TmpClass.cpp
- `ColumnTypes()`: Performs ColumnTypes operation in TmpClass.cpp
- `PutDirectoryContents()`: Checks a condition in TmpClass.cpp and returns true or false based on the result
- `FullName()`: Performs FullName operation in TmpClass.cpp
- `GoToFile()`: Performs GoToFile operation in TmpClass.cpp
- `Buffer()`: Iterates through elements in TmpClass.cpp and adds them to a collection
- `SaveListFile()`: REQUIRED, we don't know the size
### Podsumowanie
Plik `TmpClass.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 9 funkcji wspierających operacje menedżera plików Far Manager.
