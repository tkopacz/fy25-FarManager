# flink.hpp
## English
### Purpose
This file implements functionality related to: Заголовочный файл для работы с Hard & SymLink
Main functional areas: core functionality
### Key Classes
- `error_state_ex`: Performs error_state_ex operation as part of the component's functionality
### Key Functions
- `MkHardLink()`: Performs MkHardLink operation as part of the component's functionality
- `CreateVolumeMountPoint()`: Creates and initializes new VolumeMountPoint instance
- `CreateReparsePoint()`: Creates and initializes new ReparsePoint instance
- `DeleteReparsePoint()`: Removes ReparsePoint from collection and frees associated resources
- `ModifyReparsePoint()`: Performs ModifyReparsePoint operation as part of the component's functionality
- `GetReparsePointInfo()`: Retrieves ReparsePointInfo from internal state or data structure
- `GetSubstName()`: Retrieves SubstName from internal state or data structure
- `GetVHDInfo()`: Retrieves VHDInfo from internal state or data structure
- `detach_vhd()`: Performs detach_vhd operation as part of the component's functionality
- `DelSubstDrive()`: Performs DelSubstDrive operation as part of the component's functionality
### Summary
The `flink.hpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Заголовочный файл для работы с Hard & SymLink
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `error_state_ex`: Performs error_state_ex operation as part of the component's functionality
### Kluczowe Funkcje
- `MkHardLink()`: Performs MkHardLink operation as part of the component's functionality
- `CreateVolumeMountPoint()`: Tworzy i inicjalizuje nowy VolumeMountPoint instancję
- `CreateReparsePoint()`: Tworzy i inicjalizuje nowy ReparsePoint instancję
- `DeleteReparsePoint()`: Usuwa ReparsePoint z kolekcji i zwalnia powiązane zasoby
- `ModifyReparsePoint()`: Performs ModifyReparsePoint operation as part of the component's functionality
- `GetReparsePointInfo()`: Pobiera ReparsePointInfo ze stanu wewnętrznego lub struktury danych
- `GetSubstName()`: Pobiera SubstName ze stanu wewnętrznego lub struktury danych
- `GetVHDInfo()`: Pobiera VHDInfo ze stanu wewnętrznego lub struktury danych
- `detach_vhd()`: Performs detach_vhd operation as part of the component's functionality
- `DelSubstDrive()`: Performs DelSubstDrive operation as part of the component's functionality
### Podsumowanie
Plik `flink.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
