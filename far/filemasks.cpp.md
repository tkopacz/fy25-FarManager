# filemasks.cpp
## English
### Purpose
This file implements functionality related to: Класс для работы с масками файлов (учитывается наличие масок исключения).
Main functional areas: file system operations
### Key Classes
- `filemasks`: Performs filemasks operation in filemasks.cpp
- `regex_data`: Performs regex_data operation in filemasks.cpp
- `with_brackets`: Performs with_brackets operation in filemasks.cpp
### Key Functions
- `extract_impl()`: Performs extract_impl operation in filemasks.cpp
- `extract_separators()`: Performs extract_separators operation in filemasks.cpp
- `extract_masks()`: Performs extract_masks operation in filemasks.cpp
- `extract_re()`: Performs extract_re operation in filemasks.cpp
- `assign()`: Iterates through elements in filemasks.cpp and adds them to a collection
- `empty()`: Checks a condition in filemasks.cpp and returns true or false based on the result
- `last_matches()`: It holds regex_data only when compilation is successful
- `get_mask_group()`: Retrieves _mask_group value in filemasks.cpp
- `ExpandedGroups()`: Performs ExpandedGroups operation in filemasks.cpp
- `add_pathext()`: Checks a condition in filemasks.cpp and returns true or false based on the result
### Summary
The `filemasks.cpp` file provides essential functionality for file system operations. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Класс для работы с масками файлов (учитывается наличие масок исключения).
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Klasy
- `filemasks`: Performs filemasks operation in filemasks.cpp
- `regex_data`: Performs regex_data operation in filemasks.cpp
- `with_brackets`: Performs with_brackets operation in filemasks.cpp
### Kluczowe Funkcje
- `extract_impl()`: Performs extract_impl operation in filemasks.cpp
- `extract_separators()`: Performs extract_separators operation in filemasks.cpp
- `extract_masks()`: Performs extract_masks operation in filemasks.cpp
- `extract_re()`: Performs extract_re operation in filemasks.cpp
- `assign()`: Iterates through elements in filemasks.cpp and adds them to a collection
- `empty()`: Checks a condition in filemasks.cpp and returns true or false based on the result
- `last_matches()`: It holds regex_data only when compilation is successful
- `get_mask_group()`: Retrieves _mask_group value in filemasks.cpp
- `ExpandedGroups()`: Performs ExpandedGroups operation in filemasks.cpp
- `add_pathext()`: Checks a condition in filemasks.cpp and returns true or false based on the result
### Podsumowanie
Plik `filemasks.cpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
