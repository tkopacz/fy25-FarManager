# hdb_mem.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `DeleteAll()`: Removes All from collection and frees associated resources
- `AddNew()`: Adds new New to collection or list
- `AddINT()`: Adds new INT to collection or list
- `DeleteNum()`: Removes Num from collection and frees associated resources
- `Item()`: Performs Item operation as part of the component's functionality
- `Init()`: Initializes data structures and sets up initial state for operation
- `Sort()`: Performs Sort operation as part of the component's functionality
- `Search()`: Searches for item in collection and returns position or reference
- `SearchINT()`: Searches for INT in collection and returns position or reference
- `LSearch()`: Performs LSearch operation as part of the component's functionality
### Summary
The `hdb_mem.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `DeleteAll()`: Usuwa All z kolekcji i zwalnia powiązane zasoby
- `AddNew()`: Dodaje nowy New do kolekcji lub listy
- `AddINT()`: Dodaje nowy INT do kolekcji lub listy
- `DeleteNum()`: Usuwa Num z kolekcji i zwalnia powiązane zasoby
- `Item()`: Performs Item operation as part of the component's functionality
- `Init()`: Inicjalizuje struktury danych i ustawia stan początkowy dla operacji
- `Sort()`: Performs Sort operation as part of the component's functionality
- `Search()`: Wyszukuje element w kolekcji i zwraca pozycję lub referencję
- `SearchINT()`: Wyszukuje INT w kolekcji i zwraca pozycję lub referencję
- `LSearch()`: Performs LSearch operation as part of the component's functionality
### Podsumowanie
Plik `hdb_mem.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
