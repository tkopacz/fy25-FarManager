# poscache.hpp
## English
### Purpose
This file implements functionality related to: Кэш позиций в файлах для viewer/editor
Main functional areas: file caching
### Key Classes
- `Bookmarks`: Количество закладок в редакторе/вьювере на одну позицию
- `EditorPosCache`: Implements EditorPosCache functionality
- `FilePositionCache`: Implements FilePositionCache functionality
- `ViewerPosCache`: Implements ViewerPosCache functionality
- `editor_bookmark`: Implements editor_bookmark functionality
- `viewer_bookmark`: Implements viewer_bookmark functionality
### Key Functions
- `Clear()`: Количество закладок в редакторе/вьювере на одну позицию
- `AddPosition()`: Implements AddPosition functionality
- `GetPosition()`: Retrieves Position from current context or object state
- `CompactHistory()`: Implements CompactHistory functionality
### Summary
The `poscache.hpp` file provides essential functionality for file caching. It defines 6 class(es) and contains approximately 4 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Кэш позиций в файлах для viewer/editor
Główne obszary funkcjonalne: buforowanie plików
### Kluczowe Klasy
- `Bookmarks`: Количество закладок в редакторе/вьювере на одну позицию
- `EditorPosCache`: Implements EditorPosCache functionality
- `FilePositionCache`: Implements FilePositionCache functionality
- `ViewerPosCache`: Implements ViewerPosCache functionality
- `editor_bookmark`: Implements editor_bookmark functionality
- `viewer_bookmark`: Implements viewer_bookmark functionality
### Kluczowe Funkcje
- `Clear()`: Количество закладок в редакторе/вьювере на одну позицию
- `AddPosition()`: Implements AddPosition functionality
- `GetPosition()`: Retrieves Position from current context or object state
- `CompactHistory()`: Implements CompactHistory functionality
### Podsumowanie
Plik `poscache.hpp` zapewnia podstawową funkcjonalność dla buforowanie plików. Definiuje 6 klas(y) i zawiera około 4 funkcji wspierających operacje menedżera plików Far Manager.
