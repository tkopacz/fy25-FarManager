# fielddef.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `HFieldBitNameByNumber()`: Checks a condition in fielddef.cpp and returns true or false based on the result
- `HFieldCheckValueInt()`: Checks a condition in fielddef.cpp and returns true or false based on the result
- `DoEnumItem()`: Checks a condition in fielddef.cpp and returns true or false based on the result
- `HFieldEnum()`: Checks a condition in fielddef.cpp and returns true or false based on the result
- `HFieldItemEnum()`: Checks a condition in fielddef.cpp and returns true or false based on the result
### Summary
The `fielddef.cpp` file provides essential functionality for core functionality. and contains approximately 5 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `HFieldBitNameByNumber()`: Checks a condition in fielddef.cpp and returns true or false based on the result
- `HFieldCheckValueInt()`: Checks a condition in fielddef.cpp and returns true or false based on the result
- `DoEnumItem()`: Checks a condition in fielddef.cpp and returns true or false based on the result
- `HFieldEnum()`: Checks a condition in fielddef.cpp and returns true or false based on the result
- `HFieldItemEnum()`: Checks a condition in fielddef.cpp and returns true or false based on the result
### Podsumowanie
Plik `fielddef.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 5 funkcji wspierających operacje menedżera plików Far Manager.
