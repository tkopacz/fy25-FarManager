# 7z.Class.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `FormatInfo`: Evaluates conditions and returns a boolean indicating success or validity of FormatInfo
### Key Functions
- `GetFormatCommand()`: Retrieves FormatCommand from internal state or data structure
- `FindFormats()`: Searches for Formats in collection and returns position or reference
- `ConvertToFormatInfo()`: Converts data from one format or type to another
- `CInFile()`: Performs CInFile operation as part of the component's functionality
- `CArchiveOpenCallback()`: Callback function that handles specific events or notifications
- `VariantToInt64()`: Performs VariantToInt64 operation as part of the component's functionality
- `COutFile()`: Performs COutFile operation as part of the component's functionality
- `CArchiveUpdateCallback()`: Callback function that handles specific events or notifications
- `DeleteFile()`: Removes File from collection and frees associated resources
- `GetIndex()`: Retrieves Index from internal state or data structure
### Summary
The `7z.Class.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `FormatInfo`: Evaluates conditions and returns a boolean indicating success or validity of FormatInfo
### Kluczowe Funkcje
- `GetFormatCommand()`: Pobiera FormatCommand ze stanu wewnętrznego lub struktury danych
- `FindFormats()`: Wyszukuje Formats w kolekcji i zwraca pozycję lub referencję
- `ConvertToFormatInfo()`: Konwertuje dane z jednego formatu lub typu do innego
- `CInFile()`: Performs CInFile operation as part of the component's functionality
- `CArchiveOpenCallback()`: Callback function that handles specific events or notifications
- `VariantToInt64()`: Performs VariantToInt64 operation as part of the component's functionality
- `COutFile()`: Performs COutFile operation as part of the component's functionality
- `CArchiveUpdateCallback()`: Callback function that handles specific events or notifications
- `DeleteFile()`: Usuwa File z kolekcji i zwalnia powiązane zasoby
- `GetIndex()`: Pobiera Index ze stanu wewnętrznego lub struktury danych
### Podsumowanie
Plik `7z.Class.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
