# RegExp.cpp
## English
### Purpose
This file implements functionality related to: Regular expressions
Main functional areas: regular expressions
### Key Classes
- `REOpCode_data`: Performs REOpCode_data operation in RegExp.cpp
- `RegExp`: Performs RegExp operation in RegExp.cpp
- `SAlternative`: Performs SAlternative operation in RegExp.cpp
- `SAssert`: Performs SAssert operation in RegExp.cpp
- `SBracket`: Performs SBracket operation in RegExp.cpp
- `SNamedBracket`: Performs SNamedBracket operation in RegExp.cpp
- `SRange`: Performs SRange operation in RegExp.cpp
- `Setter`: Sets or updates ter value in RegExp.cpp
- `tests`: Performs tests operation in RegExp.cpp
### Key Functions
- `isType()`: Checks a condition in RegExp.cpp and returns the result
- `Reset()`: Performs Reset operation in RegExp.cpp
- `bool()`: Performs bool operation in RegExp.cpp
- `GetBit()`: Retrieves Bit value in RegExp.cpp
- `Setter()`: Sets or updates ter value in RegExp.cpp
- `SetBit()`: Sets or updates Bit value in RegExp.cpp
- `ClearBit()`: Performs ClearBit operation in RegExp.cpp
- `regex_exception()`: Validates a condition in RegExp.cpp and throws an exception if the validation fails
### Summary
The `RegExp.cpp` file provides essential functionality for regular expressions. It defines 9 class(es) and contains approximately 8 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Regular expressions
Główne obszary funkcjonalne: wyrażenia regularne
### Kluczowe Klasy
- `REOpCode_data`: Performs REOpCode_data operation in RegExp.cpp
- `RegExp`: Performs RegExp operation in RegExp.cpp
- `SAlternative`: Performs SAlternative operation in RegExp.cpp
- `SAssert`: Performs SAssert operation in RegExp.cpp
- `SBracket`: Performs SBracket operation in RegExp.cpp
- `SNamedBracket`: Performs SNamedBracket operation in RegExp.cpp
- `SRange`: Performs SRange operation in RegExp.cpp
- `Setter`: Sets or updates ter value in RegExp.cpp
- `tests`: Performs tests operation in RegExp.cpp
### Kluczowe Funkcje
- `isType()`: Checks a condition in RegExp.cpp and returns the result
- `Reset()`: Performs Reset operation in RegExp.cpp
- `bool()`: Performs bool operation in RegExp.cpp
- `GetBit()`: Retrieves Bit value in RegExp.cpp
- `Setter()`: Sets or updates ter value in RegExp.cpp
- `SetBit()`: Sets or updates Bit value in RegExp.cpp
- `ClearBit()`: Performs ClearBit operation in RegExp.cpp
- `regex_exception()`: Validates a condition in RegExp.cpp and throws an exception if the validation fails
### Podsumowanie
Plik `RegExp.cpp` zapewnia podstawową funkcjonalność dla wyrażenia regularne. Definiuje 9 klas(y) i zawiera około 8 funkcji wspierających operacje menedżera plików Far Manager.
