# newarc.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `STR2GUID()`: Constructor that initializes STR2GUID object with provided parameters
- `OpenFilePlugin()`: Opens resource and prepares it for access operations
- `ArchivePanel()`: Performs ArchivePanel operation as part of the component's functionality
- `LoadAndInitializePlugins()`: Loads data or resources from storage into memory
- `GetFindData()`: Retrieves FindData from internal state or data structure
- `FreeFindData()`: Performs FreeFindData operation as part of the component's functionality
- `ClosePlugin()`: Closes resource and performs cleanup operations
- `GetOpenPluginInfo()`: Retrieves OpenPluginInfo from internal state or data structure
- `GetFiles()`: Retrieves Files from internal state or data structure
- `PutFiles()`: Performs PutFiles operation as part of the component's functionality
### Summary
The `newarc.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `STR2GUID()`: Konstruktor inicjalizujący STR2GUID obiekt z dostarczonymi parametrami
- `OpenFilePlugin()`: Otwiera zasób i przygotowuje go do operacji dostępu
- `ArchivePanel()`: Performs ArchivePanel operation as part of the component's functionality
- `LoadAndInitializePlugins()`: Ładuje dane lub zasoby z magazynu do pamięci
- `GetFindData()`: Pobiera FindData ze stanu wewnętrznego lub struktury danych
- `FreeFindData()`: Performs FreeFindData operation as part of the component's functionality
- `ClosePlugin()`: Zamyka zasób i wykonuje operacje czyszczące
- `GetOpenPluginInfo()`: Pobiera OpenPluginInfo ze stanu wewnętrznego lub struktury danych
- `GetFiles()`: Pobiera Files ze stanu wewnętrznego lub struktury danych
- `PutFiles()`: Performs PutFiles operation as part of the component's functionality
### Podsumowanie
Plik `newarc.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
