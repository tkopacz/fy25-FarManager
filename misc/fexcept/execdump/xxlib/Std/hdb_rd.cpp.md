# hdb_rd.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `ReadInfo()`: Reads data from source into provided buffer
- `FindBase()`: Searches for Base in collection and returns position or reference
- `IsTheSame()`: Tests whether TheSame condition is true or property exists
- `NewRecord()`: Evaluates conditions and returns a boolean indicating success or validity of NewRecord
- `NewRecordCvt()`: Evaluates conditions and returns a boolean indicating success or validity of NewRecordCvt
- `FindColumn()`: Searches for Column in collection and returns position or reference
- `Read()`: Reads data from source into provided buffer
- `Skip()`: Evaluates conditions and returns a boolean indicating success or validity of Skip
- `CreateInfos()`: Creates and initializes new Infos instance
- `idDelRecBuff()`: Performs idDelRecBuff operation as part of the component's functionality
### Summary
The `hdb_rd.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `ReadInfo()`: Odczytuje dane ze źródła do dostarczonego bufora
- `FindBase()`: Wyszukuje Base w kolekcji i zwraca pozycję lub referencję
- `IsTheSame()`: Testuje czy TheSame warunek jest prawdziwy lub właściwość istnieje
- `NewRecord()`: Evaluates conditions and returns a boolean indicating success or validity of NewRecord
- `NewRecordCvt()`: Evaluates conditions and returns a boolean indicating success or validity of NewRecordCvt
- `FindColumn()`: Wyszukuje Column w kolekcji i zwraca pozycję lub referencję
- `Read()`: Odczytuje dane ze źródła do dostarczonego bufora
- `Skip()`: Evaluates conditions and returns a boolean indicating success or validity of Skip
- `CreateInfos()`: Tworzy i inicjalizuje nowy Infos instancję
- `idDelRecBuff()`: Performs idDelRecBuff operation as part of the component's functionality
### Podsumowanie
Plik `hdb_rd.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
