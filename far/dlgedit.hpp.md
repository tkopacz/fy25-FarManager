# dlgedit.hpp
## English
### Purpose
This file implements functionality related to: Одиночная строка редактирования для диалога (как наследник класса Edit)
Main functional areas: dialog handling, text editing
### Key Classes
- `Dialog`: Performs Dialog operation in dlgedit.hpp
- `EditControl`: Performs EditControl operation in dlgedit.hpp
- `Editor`: Performs Editor operation in dlgedit.hpp
- `FarColor`: Performs FarColor operation in dlgedit.hpp
- `History`: Performs History operation in dlgedit.hpp
- `SetAutocomplete`: Sets or updates Autocomplete value in dlgedit.hpp
### Key Functions
- `Flags()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `Init()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `ProcessKey()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `ProcessMouse()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `Show()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `SetPosition()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `GetPosition()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `Hide()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `ShowConsoleTitle()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `SetScreenPosition()`: Iterates through elements in dlgedit.hpp and adds them to a collection
### Summary
The `dlgedit.hpp` file provides essential functionality for dialog handling, text editing. It defines 6 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Одиночная строка редактирования для диалога (как наследник класса Edit)
Główne obszary funkcjonalne: obsługa dialogów, edycja tekstu
### Kluczowe Klasy
- `Dialog`: Performs Dialog operation in dlgedit.hpp
- `EditControl`: Performs EditControl operation in dlgedit.hpp
- `Editor`: Performs Editor operation in dlgedit.hpp
- `FarColor`: Performs FarColor operation in dlgedit.hpp
- `History`: Performs History operation in dlgedit.hpp
- `SetAutocomplete`: Sets or updates Autocomplete value in dlgedit.hpp
### Kluczowe Funkcje
- `Flags()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `Init()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `ProcessKey()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `ProcessMouse()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `Show()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `SetPosition()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `GetPosition()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `Hide()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `ShowConsoleTitle()`: Iterates through elements in dlgedit.hpp and adds them to a collection
- `SetScreenPosition()`: Iterates through elements in dlgedit.hpp and adds them to a collection
### Podsumowanie
Plik `dlgedit.hpp` zapewnia podstawową funkcjonalność dla obsługa dialogów, edycja tekstu. Definiuje 6 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
