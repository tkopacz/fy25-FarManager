# custom.cpp
## English
### Purpose
This file implements functionality related to: Second-level plugin module for FAR Manager and MultiArc plugin
Main functional areas: core functionality
### Key Classes
- `Meta`: Performs Meta operation as part of the component's functionality
- `MetaReplacer`: Performs MetaReplacer operation as part of the component's functionality
- `StringList`: Performs StringList operation as part of the component's functionality
### Key Functions
- `OpenArchivePipe()`: Opens resource and prepares it for access operations
- `GetString()`: Retrieves String from internal state or data structure
- `HexCharToNum()`: Performs HexCharToNum operation as part of the component's functionality
- `GetSectionName()`: Retrieves SectionName from internal state or data structure
- `GetIniString()`: Retrieves IniString from internal state or data structure
- `GetIniInt()`: Retrieves IniInt from internal state or data structure
- `FillFormat()`: Allocates memory and initializes resources for FillFormat
- `MakeFiletime()`: Performs MakeFiletime operation as part of the component's functionality
- `StringToInt()`: Performs StringToInt operation as part of the component's functionality
- `StringToInt64()`: Performs StringToInt64 operation as part of the component's functionality
### Namespaces
- `PCRE`
### Summary
The `custom.cpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Second-level plugin module for FAR Manager and MultiArc plugin
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `Meta`: Performs Meta operation as part of the component's functionality
- `MetaReplacer`: Performs MetaReplacer operation as part of the component's functionality
- `StringList`: Performs StringList operation as part of the component's functionality
### Kluczowe Funkcje
- `OpenArchivePipe()`: Otwiera zasób i przygotowuje go do operacji dostępu
- `GetString()`: Pobiera String ze stanu wewnętrznego lub struktury danych
- `HexCharToNum()`: Performs HexCharToNum operation as part of the component's functionality
- `GetSectionName()`: Pobiera SectionName ze stanu wewnętrznego lub struktury danych
- `GetIniString()`: Pobiera IniString ze stanu wewnętrznego lub struktury danych
- `GetIniInt()`: Pobiera IniInt ze stanu wewnętrznego lub struktury danych
- `FillFormat()`: Allocates memory and initializes resources for FillFormat
- `MakeFiletime()`: Performs MakeFiletime operation as part of the component's functionality
- `StringToInt()`: Performs StringToInt operation as part of the component's functionality
- `StringToInt64()`: Performs StringToInt64 operation as part of the component's functionality
### Przestrzenie nazw
- `PCRE`
### Podsumowanie
Plik `custom.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
