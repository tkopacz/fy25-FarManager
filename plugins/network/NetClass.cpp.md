# NetClass.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `settings()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `OpenLogFile()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `AddConnection()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `LogData()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `RemoveItems()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `Builder()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `SetCurrentDirectory()`: change to the root of the drive where network.dll resides
- `DisconnectFromServer()`: Try to search login info in registry
- `lstrcpy()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `AddConnectionWithLogon()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
### Summary
The `NetClass.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `settings()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `OpenLogFile()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `AddConnection()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `LogData()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `RemoveItems()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `Builder()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `SetCurrentDirectory()`: change to the root of the drive where network.dll resides
- `DisconnectFromServer()`: Try to search login info in registry
- `lstrcpy()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
- `AddConnectionWithLogon()`: Opens and manages a file in NetClass.cpp, handling resource cleanup
### Podsumowanie
Plik `NetClass.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
