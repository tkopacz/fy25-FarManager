# d5d.Plugin.cpp
## English
### Purpose
This file implements functionality related to: m_pfnGetDriverInfo(pInfo);
Main functional areas: plugin interface
### Key Functions
- `ShortStringToTCHAR()`: Implements ShortStringToTCHAR functionality
- `GetLanguageString()`: Retrieves LanguageString from current context or object state
- `ShowMessageBox()`: Implements ShowMessageBox functionality
- `Percent()`: Iterates through collection and processes each element, storing results
- `GetLanguageStringThunk()`: Iterates through collection and processes each element, storing results
- `ShowMessageBoxThunk()`: Iterates through collection and processes each element, storing results
- `PercentThunk()`: not supported, TStream needed __declspec (naked) void ShowMessageBoxThunk()
- `m_pfnGetNumVersion()`: Iterates through collection and processes each element, storing results
- `ANSI_NAME_DELETE()`: Iterates through collection and processes each element, storing results
- `D5DArchive()`: Iterates through collection and processes each element, storing results
### Summary
The `d5d.Plugin.cpp` file provides essential functionality for plugin interface. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: m_pfnGetDriverInfo(pInfo);
Główne obszary funkcjonalne: interfejs wtyczek
### Kluczowe Funkcje
- `ShortStringToTCHAR()`: Implements ShortStringToTCHAR functionality
- `GetLanguageString()`: Retrieves LanguageString from current context or object state
- `ShowMessageBox()`: Implements ShowMessageBox functionality
- `Percent()`: Iterates through collection and processes each element, storing results
- `GetLanguageStringThunk()`: Iterates through collection and processes each element, storing results
- `ShowMessageBoxThunk()`: Iterates through collection and processes each element, storing results
- `PercentThunk()`: not supported, TStream needed __declspec (naked) void ShowMessageBoxThunk()
- `m_pfnGetNumVersion()`: Iterates through collection and processes each element, storing results
- `ANSI_NAME_DELETE()`: Iterates through collection and processes each element, storing results
- `D5DArchive()`: Iterates through collection and processes each element, storing results
### Podsumowanie
Plik `d5d.Plugin.cpp` zapewnia podstawową funkcjonalność dla interfejs wtyczek. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
