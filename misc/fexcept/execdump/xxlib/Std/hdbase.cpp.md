# hdbase.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `DupName()`: Implements DupName functionality
- `HDBType2Str()`: Processes input by dispatching to different code paths based on type or value
- `HDBColWidth()`: Processes input by dispatching to different code paths based on type or value
- `HDBCreate()`: Processes input by dispatching to different code paths based on type or value
- `HDBDestroy()`: Processes input by dispatching to different code paths based on type or value
- `HDBValue2Str()`: Processes input by dispatching to different code paths based on type or value
- `HDBDestroyColumns()`: Implements HDBDestroyColumns functionality
- `HDBCreateColumns()`: Implements HDBCreateColumns functionality
- `HDBFillColumns()`: Implements HDBFillColumns functionality
- `HDBCharConvert()`: Implements HDBCharConvert functionality
### Summary
The `hdbase.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `DupName()`: Implements DupName functionality
- `HDBType2Str()`: Processes input by dispatching to different code paths based on type or value
- `HDBColWidth()`: Processes input by dispatching to different code paths based on type or value
- `HDBCreate()`: Processes input by dispatching to different code paths based on type or value
- `HDBDestroy()`: Processes input by dispatching to different code paths based on type or value
- `HDBValue2Str()`: Processes input by dispatching to different code paths based on type or value
- `HDBDestroyColumns()`: Implements HDBDestroyColumns functionality
- `HDBCreateColumns()`: Implements HDBCreateColumns functionality
- `HDBFillColumns()`: Implements HDBFillColumns functionality
- `HDBCharConvert()`: Implements HDBCharConvert functionality
### Podsumowanie
Plik `hdbase.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
