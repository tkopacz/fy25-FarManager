# 7z.Class.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `FormatInfo`: Checks a condition in 7z.Class.cpp and returns true or false based on the result
### Key Functions
- `GetFormatCommand()`: Checks a condition in 7z.Class.cpp and returns true or false based on the result
- `FindFormats()`: else if ( IsEqualGUID(guid, CLSID_CChmHandler) )
- `ConvertToFormatInfo()`: Checks a condition in 7z.Class.cpp and returns true or false based on the result
- `CInFile()`: Performs CInFile operation in 7z.Class.cpp
- `CArchiveOpenCallback()`: Performs CArchiveOpenCallback operation in 7z.Class.cpp
- `VariantToInt64()`: Checks a condition in 7z.Class.cpp and returns true or false based on the result
- `COutFile()`: Performs COutFile operation in 7z.Class.cpp
- `CArchiveUpdateCallback()`: Performs CArchiveUpdateCallback operation in 7z.Class.cpp
- `DeleteFile()`: MessageBox (0, szTempName, m_lpFileName, MB_OK);
- `GetIndex()`: MessageBox (0, szTempName, m_lpFileName, MB_OK);
### Summary
The `7z.Class.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `FormatInfo`: Checks a condition in 7z.Class.cpp and returns true or false based on the result
### Kluczowe Funkcje
- `GetFormatCommand()`: Checks a condition in 7z.Class.cpp and returns true or false based on the result
- `FindFormats()`: else if ( IsEqualGUID(guid, CLSID_CChmHandler) )
- `ConvertToFormatInfo()`: Checks a condition in 7z.Class.cpp and returns true or false based on the result
- `CInFile()`: Performs CInFile operation in 7z.Class.cpp
- `CArchiveOpenCallback()`: Performs CArchiveOpenCallback operation in 7z.Class.cpp
- `VariantToInt64()`: Checks a condition in 7z.Class.cpp and returns true or false based on the result
- `COutFile()`: Performs COutFile operation in 7z.Class.cpp
- `CArchiveUpdateCallback()`: Performs CArchiveUpdateCallback operation in 7z.Class.cpp
- `DeleteFile()`: MessageBox (0, szTempName, m_lpFileName, MB_OK);
- `GetIndex()`: MessageBox (0, szTempName, m_lpFileName, MB_OK);
### Podsumowanie
Plik `7z.Class.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
