# history.cpp
## English
### Purpose
This file implements functionality related to: История (Alt-F8, Alt-F11, Alt-F12)
Main functional areas: singleton implementation
### Key Functions
- `empty()`: Implements empty functionality
- `check()`: Implements check functionality
- `add()`: Implements add functionality
- `remove()`: Removes or deletes specified item and releases associated resources
- `assign()`: Implements assign functionality
- `introduce_record()`: Async - should never be used in a transaction
- `GetPrev()`: Retrieves Prev from current context or object state
- `ProcessMenu()`: Processes input data through core business logic pipeline
- `msg()`: Processes input by dispatching to different code paths based on type or value
- `Builder()`: Implements Builder functionality
### Summary
The `history.cpp` file provides essential functionality for singleton implementation. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: История (Alt-F8, Alt-F11, Alt-F12)
Główne obszary funkcjonalne: implementacja singletona
### Kluczowe Funkcje
- `empty()`: Implements empty functionality
- `check()`: Implements check functionality
- `add()`: Implements add functionality
- `remove()`: Removes or deletes specified item and releases associated resources
- `assign()`: Implements assign functionality
- `introduce_record()`: Async - should never be used in a transaction
- `GetPrev()`: Retrieves Prev from current context or object state
- `ProcessMenu()`: Processes input data through core business logic pipeline
- `msg()`: Processes input by dispatching to different code paths based on type or value
- `Builder()`: Implements Builder functionality
### Podsumowanie
Plik `history.cpp` zapewnia podstawową funkcjonalność dla implementacja singletona. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
