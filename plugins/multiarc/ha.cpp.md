# ha.cpp
## English
### Purpose
This file implements functionality related to: Second-level plugin module for FAR Manager and MultiArc plugin
Main functional areas: core functionality
### Key Classes
- `HaHeader`: Implements HaHeader functionality
### Key Functions
- `UnixTimeToFileTime()`: Implements UnixTimeToFileTime functionality
- `SetFarInfo()`: Updates FarInfo with provided value and validates constraints
- `IsArchive()`: Checks condition and returns boolean indicating state
- `OpenArchive()`: Iterates through collection and processes each element, storing results
- `GetArcItem()`: Iterates through collection and processes each element, storing results
- `CloseArchive()`: Extract without paths
- `GetFormatName()`: Retrieves FormatName from current context or object state
- `GetDefaultCommands()`: Extract Extract without paths Test Delete Comment archive Comment files Convert to SFX Lock archive Protect archive Recover archive Add files
### Summary
The `ha.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 8 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Second-level plugin module for FAR Manager and MultiArc plugin
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `HaHeader`: Implements HaHeader functionality
### Kluczowe Funkcje
- `UnixTimeToFileTime()`: Implements UnixTimeToFileTime functionality
- `SetFarInfo()`: Updates FarInfo with provided value and validates constraints
- `IsArchive()`: Checks condition and returns boolean indicating state
- `OpenArchive()`: Iterates through collection and processes each element, storing results
- `GetArcItem()`: Iterates through collection and processes each element, storing results
- `CloseArchive()`: Extract without paths
- `GetFormatName()`: Retrieves FormatName from current context or object state
- `GetDefaultCommands()`: Extract Extract without paths Test Delete Comment archive Comment files Convert to SFX Lock archive Protect archive Recover archive Add files
### Podsumowanie
Plik `ha.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 8 funkcji wspierających operacje menedżera plików Far Manager.
