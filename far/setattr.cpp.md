# setattr.cpp
## English
### Purpose
This file implements functionality related to: Установка атрибутов файлов
Main functional areas: core functionality
### Key Classes
- `SetAttrDlgParam`: Updates AttrDlgParam with provided value and validates constraints
- `setarttr_progress`: Updates arttr_progress with provided value and validates constraints
- `state`: Implements state functionality
- `time_map`: Implements time_map functionality
### Key Functions
- `label_to_time_map_index()`: Processes input by dispatching to different code paths based on type or value
- `time_point_to_string()`: Implements time_point_to_string functionality
- `set_date_or_time()`: Updates _date_or_time with provided value and validates constraints
- `set_dates_and_times()`: only remove or keep, not
- `AdvancedAttributesDialog()`: Evaluates conditions and returns a boolean indicating success or validity of AdvancedAttributesDialog
- `SetAttrDlgProc()`: Compressed / Encrypted are mutually exclusive
- `update()`: Updates existing state with new values
- `construct_time()`: Implements construct_time functionality
- `local_to_utc()`: Implements local_to_utc functionality
- `symlink_type()`: Processes input by dispatching to different code paths based on type or value
### Summary
The `setattr.cpp` file provides essential functionality for core functionality. It defines 4 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Установка атрибутов файлов
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `SetAttrDlgParam`: Updates AttrDlgParam with provided value and validates constraints
- `setarttr_progress`: Updates arttr_progress with provided value and validates constraints
- `state`: Implements state functionality
- `time_map`: Implements time_map functionality
### Kluczowe Funkcje
- `label_to_time_map_index()`: Processes input by dispatching to different code paths based on type or value
- `time_point_to_string()`: Implements time_point_to_string functionality
- `set_date_or_time()`: Updates _date_or_time with provided value and validates constraints
- `set_dates_and_times()`: only remove or keep, not
- `AdvancedAttributesDialog()`: Evaluates conditions and returns a boolean indicating success or validity of AdvancedAttributesDialog
- `SetAttrDlgProc()`: Compressed / Encrypted are mutually exclusive
- `update()`: Updates existing state with new values
- `construct_time()`: Implements construct_time functionality
- `local_to_utc()`: Implements local_to_utc functionality
- `symlink_type()`: Processes input by dispatching to different code paths based on type or value
### Podsumowanie
Plik `setattr.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 4 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
