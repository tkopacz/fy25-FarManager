# Cfg.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `FLngColorDialog`: Performs FLngColorDialog operation as part of the component's functionality
### Key Functions
- `TrimLen()`: Performs TrimLen operation as part of the component's functionality
- `ReadCfg()`: Reads data from source into provided buffer
- `WriteCfg()`: Writes data from buffer to destination
- `FP_SetRegKey()`: Performs FP_SetRegKey operation as part of the component's functionality
- `ExtendedConfig()`: Displays dialog interface for user interaction and input collection
- `CDLG_WndProc()`: Displays dialog interface for user interaction and input collection
- `FAR_COLOR()`: Performs FAR_COLOR operation as part of the component's functionality
- `FP_GetColorDialog()`: Performs FP_GetColorDialog operation as part of the component's functionality
- `Config()`: Displays dialog interface for user interaction and input collection
- `timeout()`: Performs timeout operation as part of the component's functionality
### Summary
The `Cfg.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `FLngColorDialog`: Performs FLngColorDialog operation as part of the component's functionality
### Kluczowe Funkcje
- `TrimLen()`: Performs TrimLen operation as part of the component's functionality
- `ReadCfg()`: Odczytuje dane ze źródła do dostarczonego bufora
- `WriteCfg()`: Zapisuje dane z bufora do miejsca docelowego
- `FP_SetRegKey()`: Performs FP_SetRegKey operation as part of the component's functionality
- `ExtendedConfig()`: Displays dialog interface for user interaction and input collection
- `CDLG_WndProc()`: Displays dialog interface for user interaction and input collection
- `FAR_COLOR()`: Performs FAR_COLOR operation as part of the component's functionality
- `FP_GetColorDialog()`: Performs FP_GetColorDialog operation as part of the component's functionality
- `Config()`: Displays dialog interface for user interaction and input collection
- `timeout()`: Performs timeout operation as part of the component's functionality
### Podsumowanie
Plik `Cfg.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
