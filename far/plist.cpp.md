# plist.cpp
## English
### Purpose
This file implements functionality related to: Список процессов (Ctrl-W)
Main functional areas: core functionality
### Key Classes
- `ProcInfo`: Performs ProcInfo operation as part of the component's functionality
- `menu_data`: Performs menu_data operation as part of the component's functionality
- `proc_item`: Performs proc_item operation as part of the component's functionality
### Key Functions
- `is_cloaked_window()`: Tests whether _cloaked_window condition is true or property exists
- `is_alttab_window()`: Tests whether _alttab_window condition is true or property exists
- `EnumWindowsProc()`: Validates conditions and throws exceptions when errors are detected during EnumWindowsProc operation
- `AddMenuItem()`: Adds new MenuItem to collection or list
- `ShowProcessList()`: Displays content or makes visual element visible on screen
- `SwitchToWindow()`: Performs SwitchToWindow operation as part of the component's functionality
### Summary
The `plist.cpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 6 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Список процессов (Ctrl-W)
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `ProcInfo`: Performs ProcInfo operation as part of the component's functionality
- `menu_data`: Performs menu_data operation as part of the component's functionality
- `proc_item`: Performs proc_item operation as part of the component's functionality
### Kluczowe Funkcje
- `is_cloaked_window()`: Testuje czy _cloaked_window warunek jest prawdziwy lub właściwość istnieje
- `is_alttab_window()`: Testuje czy _alttab_window warunek jest prawdziwy lub właściwość istnieje
- `EnumWindowsProc()`: Validates conditions and throws exceptions when errors are detected during EnumWindowsProc operation
- `AddMenuItem()`: Dodaje nowy MenuItem do kolekcji lub listy
- `ShowProcessList()`: Wyświetla zawartość lub czyni element wizualny widocznym na ekranie
- `SwitchToWindow()`: Performs SwitchToWindow operation as part of the component's functionality
### Podsumowanie
Plik `plist.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 6 funkcji wspierających operacje menedżera plików Far Manager.
