# copy_progress.hpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `copy_progress`: Performs copy_progress operation as part of the component's functionality
- `files`: Performs files operation as part of the component's functionality
- `lng`: Performs lng operation as part of the component's functionality
### Key Functions
- `IsCancelled()`: Tests whether Cancelled condition is true or property exists
- `IsTotalVisible()`: Tests whether TotalVisible condition is true or property exists
- `SetNames()`: Updates Names in internal state or configuration
- `reset_current()`: Performs reset_current operation as part of the component's functionality
- `set_current_total()`: Updates _current_total in internal state or configuration
- `set_current_copied()`: Updates _current_copied in internal state or configuration
- `set_total_files()`: Updates _total_files in internal state or configuration
- `set_total_bytes()`: Updates _total_bytes in internal state or configuration
- `add_total_bytes()`: Adds new _total_bytes to collection or list
- `skip()`: Performs skip operation as part of the component's functionality
### Summary
The `copy_progress.hpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `copy_progress`: Performs copy_progress operation as part of the component's functionality
- `files`: Performs files operation as part of the component's functionality
- `lng`: Performs lng operation as part of the component's functionality
### Kluczowe Funkcje
- `IsCancelled()`: Testuje czy Cancelled warunek jest prawdziwy lub właściwość istnieje
- `IsTotalVisible()`: Testuje czy TotalVisible warunek jest prawdziwy lub właściwość istnieje
- `SetNames()`: Aktualizuje Names w stanie wewnętrznym lub konfiguracji
- `reset_current()`: Performs reset_current operation as part of the component's functionality
- `set_current_total()`: Aktualizuje _current_total w stanie wewnętrznym lub konfiguracji
- `set_current_copied()`: Aktualizuje _current_copied w stanie wewnętrznym lub konfiguracji
- `set_total_files()`: Aktualizuje _total_files w stanie wewnętrznym lub konfiguracji
- `set_total_bytes()`: Aktualizuje _total_bytes w stanie wewnętrznym lub konfiguracji
- `add_total_bytes()`: Dodaje nowy _total_bytes do kolekcji lub listy
- `skip()`: Performs skip operation as part of the component's functionality
### Podsumowanie
Plik `copy_progress.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
