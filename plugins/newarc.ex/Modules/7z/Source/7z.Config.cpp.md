# 7z.Config.cpp
## English
### Purpose
Main functional areas: configuration management
### Key Functions
- `StrToInt()`: Performs StrToInt operation as part of the component's functionality
- `_tstoi()`: Performs _tstoi operation as part of the component's functionality
- `SevenZipCompressionConfig()`: Performs SevenZipCompressionConfig operation as part of the component's functionality
- `GetCompressionFormatInfo()`: Retrieves specific configuration value by key name
- `GetSizeString()`: Retrieves specific configuration value by key name
- `AddDictionarySize()`: Adds new DictionarySize to collection or list
- `GetDefaultDictionarySize()`: Retrieves specific configuration value by key name
- `UpdateDictionarySize()`: Updates existing data with new values while preserving structure
- `hndConfigureFormat()`: Evaluates conditions and returns a boolean indicating success or validity of hndConfigureFormat
- `dlgSevenZipPluginConfigure()`: Evaluates conditions and returns boolean indicating success or validity
### Summary
The `7z.Config.cpp` file provides essential functionality for configuration management. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: zarządzanie konfiguracją
### Kluczowe Funkcje
- `StrToInt()`: Wykonuje StrToInt operację jako część zarządzanie konfiguracją
- `_tstoi()`: Wykonuje _tstoi operację jako część zarządzanie konfiguracją
- `SevenZipCompressionConfig()`: Wykonuje SevenZipCompressionConfig operację jako część zarządzanie konfiguracją
- `GetCompressionFormatInfo()`: Pobiera specific configuration wartość by key name
- `GetSizeString()`: Pobiera specific configuration wartość by key name
- `AddDictionarySize()`: Dodaje nowy DictionarySize do kolekcji lub listy
- `GetDefaultDictionarySize()`: Pobiera specific configuration wartość by key name
- `UpdateDictionarySize()`: Aktualizuje istniejące dane nowymi wartościami zachowując strukturę
- `hndConfigureFormat()`: Wykonuje hndConfigureFormat operację jako część zarządzanie konfiguracją
- `dlgSevenZipPluginConfigure()`: Wykonuje dlgSevenZipPluginConfigure operację jako część zarządzanie konfiguracją
### Podsumowanie
Plik `7z.Config.cpp` zapewnia podstawową funkcjonalność dla zarządzanie konfiguracją. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
