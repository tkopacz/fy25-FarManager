# 7z.Archive.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `PropertyToName`: Performs PropertyToName operation in 7z.Archive.cpp
### Key Functions
- `CInFile()`: Performs CInFile operation in 7z.Archive.cpp
- `Callback()`: Performs Callback operation in 7z.Archive.cpp
- `VariantToInt64()`: Checks a condition in 7z.Archive.cpp and returns true or false based on the result
- `CreateTempName()`: Checks a condition in 7z.Archive.cpp and returns true or false based on the result
- `COutFile()`: Performs COutFile operation in 7z.Archive.cpp
- `DeleteFile()`: Deletes or removes an item or resource in 7z.Archive.cpp
- `CompareIndicies()`: Performs CompareIndicies operation in 7z.Archive.cpp
- `m_pfnCallback()`: Performs m_pfnCallback operation in 7z.Archive.cpp
### Summary
The `7z.Archive.cpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 8 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `PropertyToName`: Performs PropertyToName operation in 7z.Archive.cpp
### Kluczowe Funkcje
- `CInFile()`: Performs CInFile operation in 7z.Archive.cpp
- `Callback()`: Performs Callback operation in 7z.Archive.cpp
- `VariantToInt64()`: Checks a condition in 7z.Archive.cpp and returns true or false based on the result
- `CreateTempName()`: Checks a condition in 7z.Archive.cpp and returns true or false based on the result
- `COutFile()`: Performs COutFile operation in 7z.Archive.cpp
- `DeleteFile()`: Deletes or removes an item or resource in 7z.Archive.cpp
- `CompareIndicies()`: Performs CompareIndicies operation in 7z.Archive.cpp
- `m_pfnCallback()`: Performs m_pfnCallback operation in 7z.Archive.cpp
### Podsumowanie
Plik `7z.Archive.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 8 funkcji wspierających operacje menedżera plików Far Manager.
