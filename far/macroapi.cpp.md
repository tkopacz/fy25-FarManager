# macroapi.cpp
## English
### Purpose
This file implements functionality related to: Macro API
Main functional areas: core functionality
### Key Classes
- `FarMacroApi`: Iterates through collection and processes each element, storing results
- `LockOutput`: Processes input by dispatching to different code paths based on type or value
- `editor_options`: Implements editor_options functionality
### Key Functions
- `ToDouble()`: Processes input by dispatching to different code paths based on type or value
- `SelectPanel()`: Iterates through collection and processes each element, storing results
- `PushBoolean()`: Iterates through collection and processes each element, storing results
- `PushPointer()`: Iterates through collection and processes each element, storing results
- `PushValue()`: Iterates through collection and processes each element, storing results
- `PushNil()`: Iterates through collection and processes each element, storing results
- `PushTable()`: Iterates through collection and processes each element, storing results
- `SetTable()`: Iterates through collection and processes each element, storing results
- `PushError()`: Iterates through collection and processes each element, storing results
- `SetField()`: Iterates through collection and processes each element, storing results
### Summary
The `macroapi.cpp` file provides essential functionality for core functionality. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Macro API
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `FarMacroApi`: Iterates through collection and processes each element, storing results
- `LockOutput`: Processes input by dispatching to different code paths based on type or value
- `editor_options`: Implements editor_options functionality
### Kluczowe Funkcje
- `ToDouble()`: Processes input by dispatching to different code paths based on type or value
- `SelectPanel()`: Iterates through collection and processes each element, storing results
- `PushBoolean()`: Iterates through collection and processes each element, storing results
- `PushPointer()`: Iterates through collection and processes each element, storing results
- `PushValue()`: Iterates through collection and processes each element, storing results
- `PushNil()`: Iterates through collection and processes each element, storing results
- `PushTable()`: Iterates through collection and processes each element, storing results
- `SetTable()`: Iterates through collection and processes each element, storing results
- `PushError()`: Iterates through collection and processes each element, storing results
- `SetField()`: Iterates through collection and processes each element, storing results
### Podsumowanie
Plik `macroapi.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
