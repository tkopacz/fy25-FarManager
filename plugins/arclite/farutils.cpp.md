# farutils.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `init()`: Iterates through elements in farutils.cpp and adds them to a collection
- `get_plugin_module_path()`: Iterates through elements in farutils.cpp and adds them to a collection
- `msg_ptr()`: Iterates through elements in farutils.cpp and adds them to a collection
- `get_msg()`: Iterates through elements in farutils.cpp and adds them to a collection
- `get_optimal_msg_width()`: Iterates through elements in farutils.cpp and adds them to a collection
- `message()`: Iterates through elements in farutils.cpp and adds them to a collection
- `menu()`: Iterates through elements in farutils.cpp and adds them to a collection
- `get_progress_bar_str()`: Iterates through elements in farutils.cpp and adds them to a collection
- `set_progress_state()`: Iterates through elements in farutils.cpp and adds them to a collection
- `set_progress_value()`: Iterates through elements in farutils.cpp and adds them to a collection
### Namespaces
- `Far`
### Summary
The `farutils.cpp` file provides essential functionality for core functionality. and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `init()`: Iterates through elements in farutils.cpp and adds them to a collection
- `get_plugin_module_path()`: Iterates through elements in farutils.cpp and adds them to a collection
- `msg_ptr()`: Iterates through elements in farutils.cpp and adds them to a collection
- `get_msg()`: Iterates through elements in farutils.cpp and adds them to a collection
- `get_optimal_msg_width()`: Iterates through elements in farutils.cpp and adds them to a collection
- `message()`: Iterates through elements in farutils.cpp and adds them to a collection
- `menu()`: Iterates through elements in farutils.cpp and adds them to a collection
- `get_progress_bar_str()`: Iterates through elements in farutils.cpp and adds them to a collection
- `set_progress_state()`: Iterates through elements in farutils.cpp and adds them to a collection
- `set_progress_value()`: Iterates through elements in farutils.cpp and adds them to a collection
### Przestrzenie nazw
- `Far`
### Podsumowanie
Plik `farutils.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
