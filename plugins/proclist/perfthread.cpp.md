# perfthread.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Classes
- `PROCLIST_SYSTEM_PROCESS_INFORMATION`: Implements PROCLIST_SYSTEM_PROCESS_INFORMATION functionality
- `RegKey`: A wrapper class to provide auto-closing of registry key look backwards for the counter number 64-bit programs run only on Win64
### Key Functions
- `HKEY()`: look backwards for the counter number 64-bit programs run only on Win64 32-bit programs run on both 32-bit and 64-bit Windows so must sniff
- `getcounter()`: look backwards for the counter number 64-bit programs run only on Win64 32-bit programs run on both 32-bit and 64-bit Windows so must sniff
- `Is64BitWindows()`: look backwards for the counter number 64-bit programs run only on Win64 32-bit programs run on both 32-bit and 64-bit Windows so must sniff
- `is_wow64_itself()`: 64-bit programs run only on Win64 32-bit programs run on both 32-bit and 64-bit Windows so must sniff
- `l()`: Implements l functionality
- `get_logical_processor_count()`: Retrieves _logical_processor_count from current context or object state
- `HRESULT_FROM_WIN32()`: Implements HRESULT_FROM_WIN32 functionality
### Summary
The `perfthread.cpp` file provides essential functionality for core functionality. It defines 2 class(es) and contains approximately 7 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `PROCLIST_SYSTEM_PROCESS_INFORMATION`: Implements PROCLIST_SYSTEM_PROCESS_INFORMATION functionality
- `RegKey`: A wrapper class to provide auto-closing of registry key look backwards for the counter number 64-bit programs run only on Win64
### Kluczowe Funkcje
- `HKEY()`: look backwards for the counter number 64-bit programs run only on Win64 32-bit programs run on both 32-bit and 64-bit Windows so must sniff
- `getcounter()`: look backwards for the counter number 64-bit programs run only on Win64 32-bit programs run on both 32-bit and 64-bit Windows so must sniff
- `Is64BitWindows()`: look backwards for the counter number 64-bit programs run only on Win64 32-bit programs run on both 32-bit and 64-bit Windows so must sniff
- `is_wow64_itself()`: 64-bit programs run only on Win64 32-bit programs run on both 32-bit and 64-bit Windows so must sniff
- `l()`: Implements l functionality
- `get_logical_processor_count()`: Retrieves _logical_processor_count from current context or object state
- `HRESULT_FROM_WIN32()`: Implements HRESULT_FROM_WIN32 functionality
### Podsumowanie
Plik `perfthread.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 2 klas(y) i zawiera około 7 funkcji wspierających operacje menedżera plików Far Manager.
